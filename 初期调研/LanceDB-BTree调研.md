# LanceDB B-Tree 标量索引深度解析

## 一、概述

LanceDB 的 B-Tree 索引（`BTreeIndex`）是一种**标量索引（Scalar Index）**，用于加速对**高基数（high-cardinality）标量列**（如整数、字符串、时间戳等）的过滤查询。

> 标量索引支持**数值、字符串、布尔值和时间类型**列，能够加速 `x > 10`、`x < 10`、`x = 10` 等过滤条件，也可加速向量检索中的预过滤（prefiltering）。

与传统的数据库 B-Tree（如 MySQL InnoDB）不同，LanceDB 的 B-Tree 索引采用了**面向海量数据分析（OLAP）场景的独特设计**。其本质是一个 **`FlatIndex`**——将所有索引值和对应的行地址存储在一个排序的 `RecordBatch` 中。

| 对比维度       | 传统数据库 B-Tree | LanceDB B-Tree                  |
| ---------- | ------------ | ------------------------------- |
| **核心数据结构** | 磁盘上的多层树形结构   | 内存中的 `RecordBatch`（`FlatIndex`） |
| **查找方式**   | 树形遍历精确查找     | B-Tree 定位页 + 内存全扫描过滤            |
| **I/O 模式** | 随机读取多个节点     | 顺序读取候选数据页                       |
| **主要场景**   | OLTP（事务型）    | OLAP（分析型）                       |

## 二、整体架构：两层设计

LanceDB B-Tree 索引采用**内存 + 磁盘两层架构**)：

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                        B-Tree 索引架构                             │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    内存层 (Memory)                          │   │
│  │  ┌─────────────────────────────────────────────────────┐    │   │
│  │  │              BTreeLookup                            │    │   │
│  │  │   (轻量级 B-Tree: 页最大值 → 页号)                   │    │   │
│  │  └─────────────────────────────────────────────────────┘    │   │
│  │  ┌─────────────────────────────────────────────────────┐    │   │
│  │  │         FlatIndex (按需加载的 RecordBatch)           │    │   │
│  │  │         values 列 + row_ids 列                      │    │   │
│  │  └─────────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    磁盘层 (Disk)                           │   │
│  │  ┌───────────────────┐  ┌───────────────────────────────┐  │   │
│  │  │ page_lookup.lance │  │    page_data.lance            │  │   │
│  │  │  (查找表元数据)    │  │  (排序后的索引值 + 行ID)      │  │   │
│  │  └───────────────────┘  └───────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**两层结构的分工**：

| 层级      | 组件                  | 存储位置     | 核心职责                              |
| ------- | ------------------- | -------- | --------------------------------- |
| **内存层** | `BTreeLookup`       | 内存       | 轻量级 B-Tree，将值范围快速映射到磁盘页号          |
| **内存层** | `FlatIndex`         | 内存（按需加载） | 存储排序后的索引值和对应行 ID                  |
| **磁盘层** | `page_lookup.lance` | 磁盘       | 构建 `BTreeLookup` 所需的元数据（每页最大值→页号） |
| **磁盘层** | `page_data.lance`   | 磁盘       | 所有索引数据页，即 `FlatIndex` 的持久化形式[     |

## 三、核心数据结构详解

### 3.1 `FlatIndex`：索引的“真身”

尽管名为 B-Tree，但 LanceDB 索引在磁盘和内存中的核心是 **`FlatIndex`**，它本质上是一个包含两列的 `RecordBatch`：

```rust
// 源码位置: rust/lance-index/src/scalar/btree.rs#L79-L80
// https://github.com/lance-format/lance/blob/f869fd15/rust/lance-index/src/scalar/btree.rs#L79-L80

pub struct RecordBatch {
    schema: SchemaRef,
    columns: Vec<ArrayRef>, // 关键：这是一个 Vector，每一项代表一列
    row_count: usize,
}

// 当你通过 batch.column(0) 获取时，得到的是一整列的所有行数据（一个连续的 Array）。
// 如果你想在内存中按行获取数据（例如，获取第 5 行的所有字段）
// 则必须分别从 column(0) 取第 5 个元素
// 再从 column(1) 取第 5 个元素……

pub struct FlatIndex {
    // 列 0: 被索引的实际值 (values)
    // 列 1: 每个值对应的行地址 (row_ids)
    data: Arc<RecordBatch>,
    // ... 其他元数据
    all_addrs_map: RowAddrTreeMap,
    null_addrs_map: RowAddrTreeMap,
    df_schema: DFSchema,
}
```

**数据布局示意**：

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                    FlatIndex (RecordBatch)                         │
├──────────────────────────────────┬──────────────────────────────────┤
│       values (列 0)              │      row_ids (列 1)              │
│       (按值排序)                 │      (与 values 一一对应)         │
├──────────────────────────────────┼──────────────────────────────────┤
│              1                   │              100                 │
│              2                   │              205                 │
│              3                   │               42                 │
│              5                   │              789                 │
│              7                   │              301                 │
│              ...                 │              ...                 │
└──────────────────────────────────┴──────────────────────────────────┘
```

> 数据按 `row_id` 排序，以便使用 `RowAddrTreeMap` 数据结构高效构建位图。

### 3.2 `BTreeLookup`：内存中的“页目录”

`BTreeLookup` 是一个**驻留在内存中的轻量级 B-Tree**，其映射关系为:

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                      BTreeLookup (内存)                            │
├──────────────────────────────────┬──────────────────────────────────┤
│     Key: 每页的最大值             │    Value: 对应的磁盘页号         │
├──────────────────────────────────┼──────────────────────────────────┤
│              10                  │              0                  │
│              25                  │              1                  │
│              50                  │              2                  │
│              75                  │              3                  │
│              ...                 │              ...                │
└──────────────────────────────────┴──────────────────────────────────┘
```

给定查询条件（如 `age > 30`），通过 `BTreeLookup` 可快速定位到**可能**包含目标数据的**所有磁盘页号**。

### 3.3 `OrderableScalarValue`：全序保证

B-Tree 要求键具备**全序关系（Total Ordering）**。Lance 实现了 `OrderableScalarValue`，为所有 Arrow 标量类型（包括 decimals、timestamps、binary data）提供了统一的全序比较。

**关键规则**：**`null` 值总是排在非 `null` 值之前**。

```rust
// OrderableScalarValue 的排序逻辑（示意）
impl Ord for OrderableScalarValue {
    fn cmp(&self, other: &Self) -> Ordering {
        match (self.is_null(), other.is_null()) {
            (true, true) => Ordering::Equal,
            (true, false) => Ordering::Less,  // null < 任何非null
            (false, true) => Ordering::Greater,
            _ => self.inner.cmp(&other.inner),
        }
    }
}
```

### 3.4 多列索引

针对多列索引，LanceDB 的 `FlatIndex` 数据布局采用了**键拼接（Key Concatenation）** 的方式。

对于多列索引 `(timestamp, level)`，`FlatIndex` 的 `values` 列存储的是**组合键（Composite Key）**，而不是单一值。其数据布局在逻辑上如下：

| `values` 列 (组合键) | `row_ids` 列 |
| ---------------- | ----------- |
| `(ts1, lvl1)`    | `row_id_1`  |
| `(ts1, lvl2)`    | `row_id_2`  |
| `(ts2, lvl1)`    | `row_id_3`  |
| `(ts2, lvl2)`    | `row_id_4`  |
| ...              | ...         |

> 注意：`(ts1, lvl1)` 表示一个复合数据结构，并非简单的字符串拼接。

**组合键的排序与比较**

组合键的排序规则是**字典序（Lexicographic Order）**，即：

- 首先比较第一列 `timestamp`。

- 如果 `timestamp` 相同，再比较第二列 `level`。

这种排序方式是实现**最左前缀匹配**原则的基础。因此，`(timestamp, level)` 索引可以高效服务于：

- 仅过滤 `timestamp` 的查询

- 同时过滤 `timestamp` 和 `level` 的查询

但无法加速仅过滤 `level` 的查询。

**关键实现：`OrderableScalarValue`**

LanceDB 通过 **`OrderableScalarValue`** 类型统一处理排序。

- 它为所有 Arrow 标量类型（整数、浮点数、字符串、时间戳等）提供了统一的、可比较的表示。

- 组合键的排序和比较，正是在这个统一的 `OrderableScalarValue` 层面进行的。多列值会被编码成一个 `OrderableScalarValue` 序列，然后进行比较。

- **注意**：`null` 值在排序中总是排在非 `null` 值之前。

**其他存储结构不变**

组合键的改变**仅限于 `FlatIndex` 的 `values` 列**，索引的其他核心机制保持不变：

- **`row_ids` 列**：依然存储对应的行 ID。

- **磁盘分页**：数据仍然按 4096 行一组进行分页存储。

- **`BTreeLookup`**：内存中的查找表，其键依然是每个数据页中**组合键的最大值。

- **查询流程**：仍然是先通过 `BTreeLookup` 定位候选页，再将候选页加载到内存，最后利用 Arrow 计算函数进行精确过滤。

## 四、物理存储与页面结构

### 4.1 磁盘文件

B-Tree 索引在磁盘上由两个 Lance 格式文件构成：

| 文件                      | 内容                | 用途                 |
| ----------------------- | ----------------- | ------------------ |
| **`page_lookup.lance`** | 每页最大值 → 页号 的映射表   | 构建内存 `BTreeLookup` |
| **`page_data.lance`**   | 所有数据页（排序后的值和行 ID） | 存储索引的“真身”          |

索引文件存储在数据集的 `_indices/` 目录下，通过 `IndexMetadata` 进行追踪。

```textile
数据集目录/
├── _indices/
│   └── {index_uuid}/
│       ├── page_lookup.lance    # 查找表
│       └── page_data.lance      # 数据页
├── data/
│   └── *.lance                  # 原始数据文件
└── _manifest                    # 清单文件（含 IndexMetadata）
```

### 4.2 Lance 文件通用物理布局（适用于两个文件）

**读取顺序**：Lance 文件采用 **“尾部优先” (Footer-first)** 的读取方式，先读最后 40 字节的 Footer，再根据其中的指针读取元数据，最后按需读取数据页

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                    .lance 文件通用物理布局                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                                                               │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │              Page 0 (数据页 0)                       │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  编码后的列数据 (Encoded Column Data)        │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │              Page 1 (数据页 1)                       │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  编码后的列数据 (Encoded Column Data)        │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │              Page N (数据页 N)                       │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  编码后的列数据 (Encoded Column Data)        │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │                                                               │ │
│  │  ⚠️ 注意：不同列的数据页可以交错存储，不要求连续                   │ │
│  │  每个数据页只存储**单列**的数据                                 │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │   Global Buffers (全局缓冲区)                                 │ │
│  │   存储文件级别的辅助数据（如 Schema 定义）                         │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │   Column Metadata (列元数据)                                  │ │
│  │   Protobuf 序列化，描述每列的编码方式                            │ │
│  │   包含 Page Table: N x M x 2 矩阵                             │ │
│  │   每个单元格记录该页在文件中的 <起始位置, 长度>                     │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │   Footer (文件尾部) — 固定 40 字节                             │ │
│  │   ┌─────────────────────────────────────────────────────┐    │ │
│  │   │  0-7  │ column_meta_start         │ u64 │          │    │ │
│  │   │  8-15 │ column_meta_offsets_start │ u64 │          │    │ │
│  │   │ 16-23 │ global_buff_offsets_start │ u64 │          │    │ │
│  │   │ 24-27 │ num_global_buffers        │ u32 │          │    │ │
│  │   │ 28-31 │ num_column_metadata       │ u32 │          │    │ │
│  │   │ 32-39 │ version                   │ u64 │          │    │ │
│  │   └─────────────────────────────────────────────────────┘    │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.3 `page_data.lance` —— 索引数据体

物理存储布局如下所示：

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                    page_data.lance 物理布局                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                  Column 0: "values" 的数据页                  │ │
│  │                                                               │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │  Page 0 (values[0..4095])                           │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  Page Metadata (编码描述、长度等)            │   │    │ │
│  │  │  ├──────────────────────────────────────────────┤   │    │ │
│  │  │  │  Data Buffer (编码后的 values 数据)          │   │    │ │
│  │  │  │                                             │   │    │ │
│  │  │  │  示例 (Plain 编码, 4个 i32 值):             │   │    │ │
│  │  │  │  [0A 00 00 00] [14 00 00 00]               │   │    │ │
│  │  │  │  [14 00 00 00] [1E 00 00 00]               │   │    │ │
│  │  │  │  即: 10, 20, 20, 30                        │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │  Page 1 (values[4096..8191])                        │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  Page Metadata                               │   │    │ │
│  │  │  ├──────────────────────────────────────────────┤   │    │ │
│  │  │  │  Data Buffer (编码后的 values 数据)          │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │  ... (更多 values 数据页)                                    │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                  Column 1: "row_ids" 的数据页                 │ │
│  │                                                               │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │  Page 0 (row_ids[0..4095])                          │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  Page Metadata                               │   │    │ │
│  │  │  ├──────────────────────────────────────────────┤   │    │ │
│  │  │  │  Data Buffer (编码后的 row_ids 数据)         │   │    │ │
│  │  │  │  与 values Page 0 一一对应                   │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │  ... (更多 row_ids 数据页)                                  │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Column Metadata (列元数据)                                   │ │
│  │  ┌─────────────────────────────────────────────────────┐     │ │
│  │  │  Column 0 (values) 的 Page Table:                   │     │ │
│  │  │  ┌──────────────┬────────────┬──────────────┐     │     │ │
│  │  │  │  Page 0      │ offset=0    │ length=4096 │     │     │ │
│  │  │  │  Page 1      │ offset=4096 │ length=4096 │     │     │ │
│  │  │  │  ...         │ ...         │ ...          │     │     │ │
│  │  │  └──────────────┴────────────┴──────────────┘     │     │ │
│  │  └─────────────────────────────────────────────────────┘     │ │
│  │  ┌─────────────────────────────────────────────────────┐     │ │
│  │  │  Column 1 (row_ids) 的 Page Table:                  │     │ │
│  │  │  ┌──────────────┬────────────┬──────────────┐     │     │ │
│  │  │  │  Page 0      │ offset=8192 │ length=4096 │     │     │ │
│  │  │  │  Page 1      │ offset=12288│ length=4096 │     │     │ │
│  │  │  │  ...         │ ...         │ ...          │     │     │ │
│  │  │  └──────────────┴────────────┴──────────────┘     │     │ │
│  │  └─────────────────────────────────────────────────────┘     │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Footer (40 bytes)                                            │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```



**关键点**：

- 数据页 (Page) 是 I/O 的基本单位，每个 Page 只存储单列数据

- 不同列的数据页可以**交错存储**，不要求连续

- 每个 Page 包含 **元数据 (Metadata)** 和 **数据缓冲区 (Data Buffer)**

- 缓冲区通常按 **64 字节对齐

- 推荐 Page 大小 ≥ **8MB**

- `values` 列的 Page N 与 `row_ids` 列的 Page N 在逻辑上一一对应（相同行范围）

 

### 4.4 `page_lookup.lance` —— 页查找表``

`page_lookup.lance` 是 LanceDB B-Tree 索引的“页目录”文件，其物理存储和页面结构都遵循 Lance 列式存储格式的规范

#### 📄 逻辑结构 (Schema)

`page_lookup.lance` 的逻辑结构非常简单，只包含三列：

| 列名 (Column)        | 类型 (Type) | 描述 (Description)          |
| ------------------ | --------- | ------------------------- |
| **`keys`**         | `T`       | 索引列的数据类型，存储每个数据页的**最大值**。 |
| **`max_values`**   | `T`       | 与 `keys` 列内容相同，用于优化范围查询。  |
| **`page_numbers`** | `UInt32`  | 对应的数据页编号。                 |

> 这里的 `T` 代表被索引列的数据类型（如整数、字符串、时间戳等）。

可以看到，`page_lookup.lance` 文件本身非常小。它的数据量与 `page_data.lance` 的数据页（Page）数量成正比，而非与数据行数成正比。以一个拥有 10 亿行数据、每页 4096 行的 B-Tree 索引为例，`page_lookup.lance` 仅包含约 244,141 行记录。

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                page_lookup.lance 逻辑 Schema                        │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Field 0: "keys" (或 "max_values")                         │   │
│  │           类型: 与原始列一致 (i32/i64/String/...)          │   │
│  │           存储: 每个数据页的**最大值**                         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Field 1: "page_numbers"                                   │   │
│  │           类型: u32                                        │   │
│  │           存储: 对应的数据页编号                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ⚠️ 每行对应 page_data.lance 中的一个数据页                        │
│  数据量 = page_data.lance 的页数，通常远小于总行数                 │
└─────────────────────────────────────────────────────────────────────┘
```

#### 🏗️ 物理存储与页面结构

`page_lookup.lance` 本身是一个标准的 **Lance 格式文件**。

- **列式存储**：作为 Lance 格式文件，其内部采用列式存储。这意味着 `keys`、`max_values` 和 `page_numbers` 这三列的数据在物理上是分开存储的。

- **页面结构**：Lance 格式将数据组织成“页面”（Pages）。每个页面通常包含一列中连续的行范围。例如，`keys` 列的数据可能存储在一个或多个页面中，`page_numbers` 列也是如此。

- **文件布局**：Lance 文件从字节 0 开始线性写入，并以一个固定大小的尾部（Footer）结束。读取文件时，系统从文件尾部开始，反向导航以加载元数据，最后读取列数据。这种设计支持高效的随机访问。

简而言之，`page_lookup.lance` 在逻辑上是一个小型查找表，但在物理存储上，它是一个标准的 Lance 列式存储文件，拥有自己的页面和列式数据布局。**一个 `page_lookup.lance` 文件严格对应一个 `page_data.lance` 文件**

page_lookup.lance 物理布局如下所示：

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                   page_lookup.lance 物理布局                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                Column 0: "keys" 的数据页                      │ │
│  │                                                               │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │  Page 0 (keys[0..4095])                             │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  Page Metadata                               │   │    │ │
│  │  │  ├──────────────────────────────────────────────┤   │    │ │
│  │  │  │  Data Buffer (编码后的 keys 数据)            │   │    │ │
│  │  │  │                                             │   │    │ │
│  │  │  │  示例 (4个 i32 最大值):                     │   │    │ │
│  │  │  │  [0A 00 00 00] [1E 00 00 00]               │   │    │ │
│  │  │  │  [32 00 00 00] [46 00 00 00]               │   │    │ │
│  │  │  │  即: 10, 30, 50, 70                        │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │  ... (更多 keys 数据页)                                     │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                Column 1: "page_numbers" 的数据页              │ │
│  │                                                               │ │
│  │  ┌──────────────────────────────────────────────────────┐    │ │
│  │  │  Page 0 (page_numbers[0..4095])                     │    │ │
│  │  │  ┌──────────────────────────────────────────────┐   │    │ │
│  │  │  │  Page Metadata                               │   │    │ │
│  │  │  ├──────────────────────────────────────────────┤   │    │ │
│  │  │  │  Data Buffer (编码后的 page_numbers 数据)    │   │    │ │
│  │  │  │  与 keys Page 0 一一对应                     │   │    │ │
│  │  │  └──────────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────────┘    │ │
│  │  ... (更多 page_numbers 数据页)                             │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Column Metadata + Footer (与 page_data.lance 结构相同)      │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

**关键点**：

- 该文件数据量 = `page_data.lance` 的页数

- 例如：10 亿行 / 4096 行每页 ≈ **244,141 行**

- 文件体积小，可**完整加载到内存**构建 `BTreeLookup` 

### 4.5 两者映射关系

```textile
┌─────────────────────────────────────────────────────────────────────┐
│              page_lookup.lance ←→ page_data.lance 映射关系         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  page_lookup.lance                      page_data.lance             │
│  ┌─────────────────────┐               ┌─────────────────────────┐ │
│  │ keys    │ page_num  │               │ values  │ row_ids       │ │
│  ├─────────┼───────────┤               ├─────────┼───────────────┤ │
│  │ 10      │ 0         │ ────────────► │ 1..10   │ ID₁..ID₄₀₉₆  │ │
│  │ 30      │ 1         │ ────────────► │ 11..30  │ ID₄₀₉₇..      │ │
│  │ 50      │ 2         │ ────────────► │ 31..50  │ ...           │ │
│  │ 70      │ 3         │ ────────────► │ 51..70  │ ...           │ │
│  └─────────┴───────────┘               └─────────┴───────────────┘ │
│                                                                     │
│  page_lookup 中的 (key, page_num) 表示：                           │
│  第 page_num 个数据页中，values 列的最大值为 key                   │
│                                                                     │
│  查询 age > 30 时：                                                │
│  ① 在 page_lookup 中查找 keys > 30 → 得到 page_num: 2, 3, ...    │
│  ② 通过 Column Metadata 的 Page Table 获取这些页在文件中的 offset │
│  ③ 从 page_data.lance 中读取这些页 → 内存中精确过滤               │
└─────────────────────────────────────────────────────────────────────┘
```

## 五、索引构建流程

### 5.1 构建流程图

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                     索引构建流程 (Training)                        │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │  1. 扫描数据集                │
              │  读取目标列的所有值            │
              └───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │  2. 按值排序                  │
              │  TrainingOrdering::Values     │
              └───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │  3. 分页 (Chunking)           │
              │  每 4096 行一个数据页          │
              └───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │  4. 提取每页最大值            │
              │  构建 B-Tree 查找表元数据      │
              └───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │  5. 写入磁盘                  │
              │  page_data.lance +            │
              │  page_lookup.lance            │
              └───────────────────────────────┘
```

### 5.2 构建关键点

B-Tree 索引的构建由 `train_btree_index` 函数完成：

1. **数据排序**：要求输入数据按值排序（通过 `TrainingCriteria` 强制执行）

2. **分页**：将数据按 `DEFAULT_BTREE_BATCH_SIZE`（4096 行）分块

3. **提取元数据**：从每个数据页提取最大值

4. **写入文件**：生成 `page_lookup.lance` 和 `page_data.lance` 两个文件

> 标量索引通过 **ScalarIndexPlugin** 插件系统实现，B-Tree 是内置插件之一



## 六、索引查询流程

### 6.1 完整查询流程图

![](D:/md_files/索引/算法实现/btree/assets/7d50c8850a7121cd8a51606e4845590810100c49.png)

```textile
┌─────────────────────────────────────────────────────────────────────┐
│                        B-Tree 索引查询流程                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  用户查询: WHERE age > 30                                          │
│         │                                                           │
│         ▼                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ① 查询解析                                                │   │
│  │     将条件 age > 30 解析为 SargableQuery[reference:24]           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│         │                                                           │
│         ▼                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ② 在内存 BTreeLookup 中查找[reference:25]                       │   │
│  │     (由 page_lookup.lance 构建)                             │   │
│  │     找到 keys > 30 的所有记录 → page_num: [2, 3, ...]      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│         │                                                           │
│         ▼                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ③ 通过 Column Metadata 的 Page Table[reference:26]          │   │
│  │     获取每个 page_num 在 page_data.lance 中的:              │   │
│  │     ┌────────────┬──────────────────┬──────────────┐       │   │
│  │     │ page_num  │ 起始 offset      │ 长度         │       │   │
│  │     ├────────────┼──────────────────┼──────────────┤       │   │
│  │     │ 2         │ 0x00004000       │ 0x00001000   │       │   │
│  │     │ 3         │ 0x00005000       │ 0x00001000   │       │   │
│  │     └────────────┴──────────────────┴──────────────┘       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│         │                                                           │
│         ▼                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ④ 从 page_data.lance 中按 offset 读取候选页[reference:27]   │   │
│  │     (如有缓存则直接命中)                                    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│         │                                                           │
│         ▼                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ⑤ 内存中精确过滤[reference:28]                                  │   │
│  │     对读取的 RecordBatch 应用 Arrow filter                  │   │
│  │     返回精确的 row_ids[reference:29]                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```



### 6.2 阶段详解

#### 阶段一：查询解析

查询引擎将过滤条件解析为内部统一的 **`SargableQuery`**（可搜索查询）：

- 等值查询：`=`

- 范围查询：`>`, `>=`, `<`, `<=`, `BETWEEN`

- 集合成员查询：`IS IN`

#### 阶段二：B-Tree 定位候选页

使用内存中的 `BTreeLookup` 快速定位候选页号列表：

- 时间复杂度：**O(log P)**，其中 P 为数据页总数

- 输出：可能包含目标数据的页号列表

#### 阶段三：读取候选数据页

从磁盘（或缓存）读取候选页的实际数据)：

- **缓存优先**：先检查 `PageCache`

- **并行读取**：多个候选页并行加载，充分利用 I/O 带宽

- **按需加载**：仅读取 `BTreeLookup` 定位到的页

#### 阶段四：内存中精确过滤

将所有候选页数据合并为 `RecordBatch`，使用 **Arrow 计算函数**进行精确过滤：

| 查询类型                                 | 实现方式             | 时间复杂度    |
| ------------------------------------ | ---------------- | -------- |
| 范围查询 (`x > 5`, `x BETWEEN 1 AND 10`) | Arrow `filter`   | **O(N)** |
| 等值查询 (`x = 7`)                       | Arrow `equality` | **O(N)** |
| `IS NULL`                            | 预计算的 null bitmap | **O(1)** |
| `IS IN` (`x IN (1,2,3)`)             | Arrow `is_in`    | **O(N)** |

> 其中 **N 是索引的总行数**。虽然是 O(N) 的全量扫描，但由于数据已在内存中，且 Arrow 计算经过 **SIMD 优化**，实际性能非常高。

## 七、加载与缓存机制

### 7.1 分层加载策略

LanceDB 采用**按需、分层**的加载策略：

| 层级                | 加载时机  | 内容                                 |
| ----------------- | ----- | ---------------------------------- |
| **元数据**           | 打开表时  | 索引文件位置、大小等元信息（延迟加载）                |
| **`BTreeLookup`** | 首次查询时 | 读取 `page_lookup.lance`，构建内存 B-Tree |
| **数据页**           | 查询执行时 | 仅加载 `BTreeLookup` 定位到的候选页          |

### 7.2 内存占用估算

官方文档给出了精确的内存估算：

> 对于一张 **10 亿行**的表，B-Tree 索引的内存占用约为 **`sizeof(Scalar) * 256Ki`** 字节。

以不同数据类型为例：

| 数据类型            | `sizeof(Scalar)` | 10亿行内存占用  | 每次查询读取量 |
| --------------- | ---------------- | --------- | ------- |
| `i32` (4 bytes) | 4 bytes          | **~1 MB** | ~16 KB  |
| `i64` (8 bytes) | 8 bytes          | **~2 MB** | ~32 KB  |
| `f64` (8 bytes) | 8 bytes          | **~2 MB** | ~32 KB  |

> 每次查询通常只需读取 `sizeof(Scalar) * 4096` 字节来定位正确的行 ID。

### 7.3 缓存机制

LanceDB 通过**多级缓存**策略优化查询性能：

1. **索引缓存（Index Cache）**
   
   - `BTreeLookup` 等热数据结构常驻内存
   
   - 通过 `OpenTableBuilder::index_cache_size` 配置缓存大小

2. **页面缓存（Page Cache）**
   
   - 数据页使用 **LRU（最近最少使用）** 算法管理
   
   - 高频访问的数据页常驻内存

3. **分级缓存策略**
   
   - 对高频查询的索引块实施**预加载**
   
   - 索引结构常驻内存，原始数据按需从磁盘加载

4. **不可变文件的优势**
   
   - 索引文件是**不可变（Immutable）** 的
   
   - 缓存策略大幅简化，无需担心数据一致性问题

## 八、海量数据场景下的挑战与应对

### 8.1 核心挑战

| 挑战        | 描述                    |
| --------- | --------------------- |
| **内存占用**  | 全量加载索引数据可能消耗大量内存      |
| **扫描开销**  | O(N) 的内存全扫描在超大索引上仍有开销 |
| **数据更新**  | 增量数据需要合并和重组索引         |
| **行地址变更** | 数据文件压缩后，索引中的行 ID 需要更新 |

### 8.2 应对策略

#### 策略一：可控的内存占用

通过**“按需加载数据页”**的策略，内存占用与**查询涉及的数据量**相关，而非与**总数据量**成正比：

- 索引结构（`BTreeLookup`）常驻内存（约 1-2 MB / 10亿行）

- 原始数据页按需从磁盘加载

- 内存占用比全量加载降低 **1-2 个数量级**

#### 策略二：向量化内存计算

利用 **Arrow 的向量化计算**和 **SIMD** 指令集加速内存扫描：

- 过滤计算下推到内存中的 `RecordBatch`

- 比传统逐行处理快数个数量级

#### 策略三：`remap` 机制处理数据变更

当数据集发生**压缩（compaction）** 或 **重组**后，索引中的行 ID 映射需要更新。

`BTreeIndex` 的 `remap` 函数执行以下操作：

1. 读取旧的 B-Tree 索引文件

2. 根据新的行地址映射关系更新索引

3. 写入新的索引文件

> B-Tree 和 Bitmap 索引都支持在压缩期间进行 `remap。

#### 策略四：增量更新

B-Tree 索引支持**增量更新**，新数据可以通过 `optimize` 操作合并到索引中。

索引可以跨多个数据集版本，通过增量更新逐步构建。

## 九、设计权衡总结

| 设计决策                                   | 优势                    | 代价                        |
| -------------------------------------- | --------------------- | ------------------------- |
| **磁盘存储 `FlatIndex`，内存存 `BTreeLookup`** | 内存占用可控，支持海量数据         | 索引本身占用可观内存                |
| **数据页按需加载**                            | 查询零磁盘 I/O（命中缓存时），速度极快 | 首次查询需加载数据页                |
| **内存全扫描 (O(N))**                       | 实现简单，充分利用 SIMD        | 对超大索引，扫描本身有开销             |
| **不可变索引文件**                            | 缓存安全，并发简单             | 需要后台任务进行维护和合并（`optimize`） |
| **基于 Arrow 和 Lance 格式**                | 零拷贝，高压缩率，与生态无缝集成      | 对技术栈有要求                   |
| **固定页大小（4096 行）**                      | 实现简单，可预测              | 未来可能支持参数化配置               |

## 十、源码参考位置

| 组件             | 源码路径                                                    |
| -------------- | ------------------------------------------------------- |
| `FlatIndex` 定义 | `rust/lance-index/src/scalar/btree.rs#L79-L80           |
| B-Tree 索引构建    | `lance-index/src/scalar/btree.rs` 中的 `train_btree_index |
| `BTreeLookup`  | 内置于 `BTreeIndex` 中                                      |
| 标量索引插件         | `ScalarIndexPlugin` trait                               |
| 索引元数据          | `IndexMetadata` 结构体                                     |

## 十一、总结

LanceDB 的 B-Tree 索引是一个**为云原生、海量数据分析（OLAP）场景量身定制**的索引方案。其核心设计哲学可概括为：

> **“磁盘存数据，内存建索引，计算下推到向量化引擎”**

通过 **`FlatIndex` + `BTreeLookup`** 的两层架构，它在数据规模、查询速度和系统复杂度之间找到了巧妙的平衡点：

1. **海量数据支持**：通过“按需加载数据页”策略，内存占用与查询数据量相关而非总数据量

2. **极致查询性能**：索引常驻内存 + Arrow SIMD 向量化计算，实现亚毫秒级过滤

3. **云原生友好**：不可变文件 + Lance 列式格式，天然适配对象存储

4. **生态整合**：深度集成 Arrow 生态，支持零拷贝和高效计算

# 附录 （问题）

## B-Tree算法

LanceDB 的 B-Tree 索引在算法实现上，为了满足海量数据分析（OLAP）场景，采用了一种非常独特的设计：**它并非一个传统的、完全持久化的 B-Tree，而是一个“内存B-Tree查找表 + 磁盘上扁平数据块”的混合体**。

它通过将“索引”与“数据”分离，在内存占用和查询性能之间取得了平衡。

### 🧠 核心数据结构与算法思想

LanceDB B-Tree 索引的物理实现，主要包含以下两个核心组件：

- **`BTreeLookup` (内存中的B-Tree)**：这是一个真正的 B-Tree 结构，常驻内存。它的作用是作为一个“页目录”，其中每个**键（Key）** 是磁盘上一个数据页中的**最大值**，每个**值（Value）** 则是对应的**数据页号**。它的存在使得系统能快速定位到可能包含目标数据的磁盘页，时间复杂度为 O(log N)。

- **`FlatIndex` (磁盘上的数据页)**：所有被索引的数据，以**排序后的 `(Value, RowID)` 形式，按 4096 行一组分页（Page）存储**在磁盘的 `page_data.lance` 文件中。这实际上是一个扁平的、有序的数组结构。其 `values` 列存储索引值，`row_ids` 列存储对应的行地址。

### ⚙️ 核心流程与算法详解

#### 1. 索引构建：从排序到分页

构建 B-Tree 索引的核心是 `train_btree_index` 函数，它遵循以下步骤：

1. **数据排序**：首先，要求输入的数据**必须按照被索引列的值进行排序**。

2. **数据分页 (Chunking)**：将排好序的数据，按固定大小（`DEFAULT_BTREE_BATCH_SIZE = 4096` 行）切分成多个数据页（Pages）。

3. **提取边界值**：遍历每个数据页，提取该页的**最大值**（即该页最后一个值）。

4. **构建查找表**：将上一步提取的“最大值”和其对应的“数据页号”记录下来，形成查找表。

5. **写入磁盘**：将上述两部分内容分别写入两个 Lance 格式的文件：
   
   - `page_data.lance`：存储所有分页后的数据。
   
   - `page_lookup.lance`：存储查找表（最大值与页号的映射）。

#### 2. 查询流程：先找页，后扫描

当执行一个过滤查询（如 `age > 30`）时，流程如下：

1. **解析查询**：将查询条件解析为内部统一的 `SargableQuery` 格式。

2. **在 `BTreeLookup` 中查找**：利用内存中的 `BTreeLookup` 结构，通过二分查找快速定位到所有**可能**包含目标数据的**数据页号列表**。这是算法中唯一与 B-Tree 相关的查找步骤。

3. **按需读取磁盘**：根据上一步得到的页号列表，**只从磁盘的 `page_data.lance` 文件中读取这些特定的数据页**。

4. **内存中精确过滤**：将读取到的数据页加载到内存，形成一个或多个 `RecordBatch`，然后使用 **Arrow 的高性能计算函数**（如 `filter`）进行精确过滤。

| 查询类型                  | 实现方式             | 时间复杂度    |
| --------------------- | ---------------- | -------- |
| **范围查询** (`x > 5`)    | Arrow `filter`   | **O(N)** |
| **等值查询** (`x = 7`)    | Arrow `equality` | **O(N)** |
| **空值查询** (`IS NULL`)  | 预计算的 null 位图     | **O(1)** |
| **集合查询** (`IN (...)`) | Arrow `is_in`    | **O(N)** |

> 这里的 N 是索引总行数。虽然是 O(N)，但数据已在内存且计算被高度优化，因此速度极快。

#### 3. 数据更新与维护：`remap` 机制

在数据湖存储中，数据会因 `UPDATE`、`DELETE` 或 `COMPACTION` 等操作而发生重组，导致行的物理位置（Row ID）改变。为此，LanceDB 的 B-Tree 索引提供了 `remap` 机制。

`remap` 的工作原理是：

1. **读取旧索引**：加载旧的 B-Tree 索引文件。

2. **更新行 ID**：根据数据文件重组时产生的旧 `RowID` 到新 `RowID` 的映射关系，更新索引中所有的行 ID。

3. **重建索引**：将更新后的数据重新训练（`train`），并写入新的索引文件。

这个机制确保了在数据湖的频繁变动下，索引依然能保持正确和高效。

#### 4. 缓存策略

为了进一步提升性能，LanceDB 对索引的关键部分实施了缓存：

- **`BTreeLookup` 常驻内存**：作为查询的入口，这个结构在索引加载后便会常驻内存，以提供最快的响应。

- **数据页缓存**：从磁盘读取的 `page_data` 数据页会被缓存起来，以便后续查询可以复用，减少磁盘 I/O。

### 💎 总结

LanceDB 的 B-Tree 索引算法并非一个传统的、从根到叶的完整 B-Tree 实现。它是一个专为云原生、海量数据分析场景设计的**混合架构**：

- **内存中**，用一个轻量的 B-Tree (`BTreeLookup`) 作为“页目录”，实现快速定位。

- **磁盘上**，数据以排序的、扁平的数据页 (`FlatIndex`) 存储。

这种设计通过“**以内存空间换磁盘 I/O 时间**”的策略，在支持海量数据的同时，保证了极致的查询性能。对于你的 Iceberg 索引项目，这种“**查找表 + 数据页**”的分层思想，以及通过 `remap` 机制处理数据更新的思路，都具有很高的参考价值。

## 缓存

在内存层面，LanceDB 要确认缓存的 `page` 是否为目标数据页，核心机制是：**使用 `(文件唯一标识, 页面的起始偏移量 offset)` 作为缓存的唯一键（Key）进行匹配。**

这是一种精确的物理地址映射，而非逻辑上的“页号匹配”。以下是详细的判断流程：

### 🗝️ 第一步：生成缓存键（Cache Key）

当你通过 `BTreeLookup` 定位到一个数据页时，你会得到它的**页号**。随后，系统会通过 `page_lookup.lance` 文件，根据页号查找到该页在 `page_data.lance` 文件中的**精确起始偏移量（`offset`）**。

内存缓存使用一个复合键来唯一标识一个数据页：

**Cache Key** = (page_data.lance 文件的唯一标识符, 页面的起始偏移量 offset)

这个 `offset` 是从文件头（0 字节）开始计算的。因此，一个固定的页面，无论是第一次从磁盘加载，还是之后从缓存读取，其对应的 `(file_id, offset)` 键值都是不变的。



#### `BTreeLookup` 的缓存键：`IndexMetadataKey`

`BTreeLookup` 的缓存键很可能就是 **`IndexMetadataKey { version }`**。

- **`version` 字段**：这个版本号与索引的元数据（`IndexMetadata`）相关联。每当索引发生变更（例如添加新数据后重建），其 `version` 就会更新。

- **`IndexMetadata` 的唯一性**：每个 `IndexMetadata` 都包含如 `uuid` 等唯一标识符。因此，`IndexMetadataKey { version }` 能够精确地定位到内存中某个索引的特定版本。

整个流程可以理解为：查询时，系统会根据表和列信息找到对应的 `IndexMetadata`，然后用其 `uuid` 和 `version` 构造出 `IndexMetadataKey`。接着，系统会使用这个键在索引缓存中查找，如果找到，就说明 `BTreeLookup` 已被缓存，可以直接使用；否则，就需要从磁盘加载。



#### `FlatIndex` 使用的缓存键是 **`ScalarIndexCacheKey { uuid, fri_uuid? }`**。

- **`uuid`**：这是索引本身的唯一标识符，用于区分不同的索引实例。

- **`fri_uuid?`**：这是一个可选的字段，代表 **Fragment Reuse Index (片段复用索引)** 的 UUID。它用于更精细地定位到索引所覆盖的特定数据片段（Fragment）。

简单来说，如果一个索引只服务于一个数据片段，那么它的缓存键可能就只用 `uuid`。但如果一个索引服务于多个数据片段，那么 `fri_uuid` 就能帮助系统区分和管理这些不同片段对应的索引缓存。



### 🔍 第二步：缓存查找（Lookup）

当查询请求一个特定偏移量的页面时，流程如下：

1. **构造查询键**：使用当前索引文件的 `file_id` 和目标 `offset` 构造一个临时键。

2. **在缓存中匹配**：缓存系统（如 LRU Cache）会直接查找是否存在与该键完全匹配的条目。
   
   - **命中（Hit）**：如果存在，则直接返回该键对应的内存数据指针。此时，你**可以确定**这段内存就是所需的 `page_data.lance` 中的目标页面。
   
   - **未命中（Miss）**：如果不存在，则发起磁盘 I/O，从 `page_data.lance` 文件的 `offset` 位置读取数据页，并将其插入缓存。

### 🛡️ 第三步：数据完整性验证（可选）

在极少数情况下，如果对数据完整性有更高要求，LanceDB 可能还会进行额外校验：

- **CRC 校验**：Lance 格式的数据块通常包含校验和。在从缓存中读取数据时，可以通过重新计算校验和来验证数据是否损坏。

- **魔法字节或版本号**：检查页面开头的特定字节，可以确认它是否是一个有效的 Lance 数据页，从而避免因缓存键冲突导致的逻辑错误。

### ⚙️ 源码层面：`PageReader` 与 `RandomAccessFile`

在实现上，这些机制依赖于底层的 `PageReader`。

`PageReader` 是 Lance 格式的一个核心组件，它能够从 `RandomAccessFile`（随机访问文件）的特定 `offset` 开始，读取并解码一个完整的数据页。无论数据来自磁盘还是缓存，最终呈现给上层的都是一个标准的 `RecordBatch` 对象，而具体的物理位置细节则被封裝在 `PageInfo`（包含 `offset` 和 `length`）中。

### 💎 总结

在内存层面，判断一个 `page` 是否就是你想要的，可以归纳为：

- **确定性判断**：通过 `(文件ID + 偏移量)` 这个唯一物理键进行匹配，匹配成功则**一定**是目标页面。

- **辅助验证**：可通过页面格式特征（如校验和）进行二次确认。

这种基于“物理偏移量”的设计，使得缓存索引极其精确，避免了传统页号映射可能带来的歧义，是列式存储系统处理海量数据时的经典实践。





## 数据页存在多页相同的最大值

当 `page_lookup.lance` 查找表中出现多个数据页具有相同“最大值”时，LanceDB 的处理方式依赖于其**两阶段查询**的架构：**查找表负责“粗筛”，数据页负责“精确过滤”**。

### 📖 背景：查找表（`page_lookup.lance`）的构建

`page_data.lance` 文件存储了索引数据，并被划分为多个数据页（Page），每个数据页最多包含 **4096 行。`page_lookup.lance` 文件则记录了每个数据页的 **最大值（max value）** 及其对应的**页号（page number）**，充当“目录”。

### 🔍 处理重复最大值的核心逻辑

面对重复的最大值，LanceDB 的策略是**容忍并保留所有重复项**。

假设 `values` 列的数据如下，并按每页 4 行进行分页（为简化示例）：

- **Page 0**: `[1, 2, 3, 4]` (最大值: 4)

- **Page 1**: `[4, 5, 6, 7]` (最大值: 7)

- **Page 2**: `[7, 7, 8, 9]` (最大值: 9)

- **Page 3**: `[9, 9, 9, 10]` (最大值: 10)

那么 `page_lookup.lance` 的内容将会是：

| 最大值 (max_value) | 页号 (page_number) |
| --------------- | ---------------- |
| 4               | 0                |
| 7               | 1                |
| 9               | 2                |
| 10              | 3                |

可以看到，尽管没有重复，但如果最大值重复，查找表中就会有多个条目指向不同的数据页。

### ⚙️ 查询时的处理流程

当执行一个查询，比如 `WHERE age >= 7` 时：

1. **粗筛 (查找表)**：查询引擎会扫描 `page_lookup.lance`，找到所有“最大值 >= 7”的条目。这会返回页号 **1, 2, 3**。

2. **读取数据页**：系统根据这些页号，从 `page_data.lance` 中读取 **Page 1, Page 2, Page 3** 的全部数据到内存。

3. **精确过滤 (数据页)**：在内存中，对读取出的所有数据行（共12行）应用精确的过滤条件 `age >= 7`，从而筛选出真正满足条件的行。

### 💎 总结

LanceDB 通过这种设计巧妙地处理了重复键值问题：

- **查找表（`page_lookup.lance`）**：它的目标不是精确定位，而是**快速缩小范围**。它通过“最大值”这个上界，高效地排除掉一批不可能包含目标数据的数据页。

- **数据页（`page_data.lance`）**：它承担了**精确过滤**的职责。由于数据页会被完整读入内存，即便最大值重复，也能保证不会遗漏任何符合条件的数据。

这种“**索引粗筛 + 内存精筛**”的两阶段策略，是 LanceDB B-Tree 索引能够处理海量数据和高基数场景的关键设计。



## 增量构建

LanceDB 在合并（`optimize`）前，通常只会有一个最新的 `page_data.lance` 文件，而不是多个。这背后的原因是其独特的设计，我们可以通过一个表格来对比它与我们架构的异同：

| 特性                   | LanceDB                                                                 | 我们的架构 (Iceberg Puffin)                                                           |
| -------------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| **索引与数据的关系**         | 索引不是为每个数据片段（Fragment）单独构建的。一个索引可以覆盖一个或多个 Fragment。                      | 一个逻辑索引由多个物理 Segment 文件（Puffin）组成，每个 Segment 对应一次增量构建。                            |
| **合并前的索引文件**         | **只有一个**最新的 `page_data.lance` 文件。新的数据写入不会生成新的索引文件，而是使现有索引“过时”。          | **有多个** Puffin 文件，每个 Segment 都是独立的。                                              |
| **增量数据如何处理**         | 新数据（新的 Fragment）**不会**立即更新到索引中。查询时，系统会**并行扫描**索引数据和未索引的新数据。             | 每次增量构建都会生成一个新的 Segment Puffin 文件。查询时，需要**合并所有 Segment** 的查询结果。                   |
| **合并（`optimize`）操作** | 当执行 `table.optimize()` 时，系统会**重新构建索引**，将旧索引和新数据合并，生成一个全新的、整合了所有数据的索引文件。 | 对应的操作是**Segment 合并（Compaction）**，它会将多个旧的 Segment Puffin 文件合并成一个新的、更大的 Puffin 文件。 |
| **索引文件数量**           | 合并前通常只有**一个**索引文件（逻辑上的单文件），但合并操作本身会产生新的文件。                              | 合并前**可能有多个** Puffin 文件，合并后数量会减少。                                                 |

### 🔍 根源：索引与数据 Fragment 的关系

LanceDB 的索引不直接与单个数据 Fragment 绑定，而是作为独立的数据结构，可以覆盖一个或多个 Fragment。

- **数据以 Fragment 组织**：LanceDB 的数据由多个不可变的 Fragment 组成，每次写入（如 `add`）都会产生新的 Fragment。

- **索引独立于 Fragment**：一个索引可以覆盖多个 Fragment，它们之间的关系由元数据（如 `fragment_bitmap`）来记录。

- **增量数据通过“重新索引”处理**：新数据以新 Fragment 形式存在，查询时通过**同时查询索引和扫描新数据**来保证结果完整性。当执行 `optimize()` 时，系统会**重建索引**，使其覆盖所有数据。

### 🔧 索引合并的“合”与“并”

LanceDB 的索引合并（`optimize`）与我们架构的 Segment 合并，本质上是相同的操作：

- **合并（Merge）**：将多个索引版本或 Segment 合并成一个。

- **重新索引（Reindexing）**：`optimize` 过程会执行重新索引，将新数据包含进来。

### 💎 总结

LanceDB 的 B-Tree 索引是**逻辑上的单文件**（一个 `page_data.lance` + 一个 `page_lookup.lance`），通过**定期重建**（`optimize`）来覆盖新数据。

而我们目前的架构，采用了**多 Segment** 的设计，每个 Segment 对应一次增量构建，它们在查询时被合并。两种设计各有侧重：LanceDB 的方式在合并前索引文件数量少，管理简单；而我们多 Segment 的方式则能更精细地控制每次增量的构建成本，并支持更灵活的增量策略。
