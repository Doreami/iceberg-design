# Puffin 自定义索引模块 — 设计文档

> **定位**: 轻量级设计文档，描述索引管理模块的架构、接口、边界。不包含具体代码实现。
> **运行环境**: openGauss 数据库内核（C 语言），通过扩展/内置模块形式集成。
> **关联**: 完整规格见 [Puffin 统一设计文档 v3](./Puffin_统一设计文档_v3.md)；适配分析见 [设计问题分析与 openGauss 适配评估](./设计问题分析与openGauss适配评估.md)。

---

## 目录

1. [模块定位与边界](#1-模块定位与边界)
2. [整体架构](#2-整体架构)
3. [Puffin 文件格式概要](#3-puffin-文件格式概要)
4. [索引组织模型](#4-索引组织模型)
5. [元数据设计：index_catalog.puffin](#5-元数据设计index_catalogpuffin)
6. [索引状态机](#6-索引状态机)
7. [Iceberg 元数据交互](#7-iceberg-元数据交互)
8. [核心数据结构](#8-核心数据结构)
9. [索引管理接口（C ABI）](#9-索引管理接口c-abi)
10. [索引实现接口（插件框架）](#10-索引实现接口插件框架)
11. [索引维护接口](#11-索引维护接口)
12. [openGauss 集成点](#12-opengauss-集成点)
13. [查询流程](#13-查询流程)
14. [文件覆盖与部分失效](#14-文件覆盖与部分失效)
15. [Puffin 文件生命周期与旧文件清理](#15-puffin-文件生命周期与旧文件清理)
16. [附录：目录结构](#16-附录目录结构)

---

## 1. 模块定位与边界

### 1.1 模块职责

本模块运行在 **openGauss 数据库内核**中，负责 Iceberg 表自定义索引的**元数据管理和生命周期管理**。它不实现任何具体的索引数据结构（BTree、Bloom Filter、IVF 等），而是提供：

- 索引的**注册与发现**（有哪些索引、覆盖哪些文件、什么状态）
- 索引的**构建调度**（全量/增量构建、调用索引插件完成实际构建）
- 索引的**状态维护**（active → stale → remap → rebuild → GC）
- **Puffin 文件读写**（Reader/Writer，纯 C 实现）
- **与 Iceberg TableMetadata 的同步**（通过 HTTP REST Catalog 或内嵌轻量 metadata 读写）
- **与 openGauss 子系统的集成**（bgworker 异步构建、ANALYZE hook、pg_catalog 缓存）

### 1.2 边界图

```
                        openGauss 数据库内核
                    ┌─────────────────────────────────┐
                    │                                 │
  TableMetadata ←──┤  本模块 (索引管理)               │
  (REST / 内嵌)    │  ┌─────────────┐ ┌────────────┐ │
                    │  │IndexCatalog │ │IndexMaintnr│ │
  Manifest ←───────┤  │segment_mgr  │ │puffin_io   │ │
  (REST / 内嵌)    │  └──────┬──────┘ └────────────┘ │
                    │         │                        │
  pg_catalog ←─────┤         │  IIndexPlugin vtable   │
  (系统表缓存)       │         ▼                        │
                    │  ┌──────────────────────────┐   │
                    │  │   索引具体实现 (外部)      │   │
                    │  │ BTree / Bloom / Bitmap    │   │
                    │  │ IVF / ...                 │   │
                    │  └──────────────────────────┘   │
                    └─────────────────────────────────┘

  本模块管: 索引有哪些、状态是什么、文件在哪里、何时构建/重建
  各索引导管: 序列化格式、构建算法、值评估逻辑
  openGauss 管: 查询规划集成、WAL/恢复、并发控制、权限
```

### 1.3 运行形态与语言策略

**MVP 阶段（当前）**：Java SDK 优先 —— Puffin 读写、TableMetadata 操作、Manifest 解析均通过 Java sidecar 完成。C 侧做薄层编排。详见简易方案 §1.2。

**目标形态（后续优化）**：

| 组件 | 形态 | 语言 | 原因 |
|------|------|------|------|
| Puffin 文件 I/O | 内核内置 | C | openGauss 是 C 内核，零拷贝集成 |
| 索引构建与评估 | 内核内置 | C | 计算密集型，需与存储引擎紧密集成 |
| Iceberg metadata 读写 | HTTP REST 或内嵌轻量库 | C | 数据库内核不能嵌入 JVM |
| 异步构建调度 | openGauss bgworker | C | 复用数据库后台任务框架 |
| 索引发现缓存 | pg_catalog 系统表 | C | 规划器 O(1) 内存查找，SQL 可查 |

**为什么目标态不走 JNI 桥接**：openGauss 是 C 语言数据库内核，进程内嵌入 JVM 会带来内存爆炸（JVM 堆 256MB+）、启动延迟（2-5s）、crash 传播、信号冲突等问题。MVP 阶段通过独立 Java 进程（sidecar）规避这些问题；目标态改为 REST Catalog HTTP API 或内嵌轻量 metadata 读写库。

---

## 2. 整体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        索引管理模块 (C)                            │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────┐  ┌──────────────────┐  ┌─────────────┐ │
│  │ IndexSegmentManager │  │  IndexCatalog    │  │Puffin IO    │ │
│  │ • build_full()      │  │  • 索引发现      │  │• Reader     │ │
│  │ • build_incremental │  │  • group/segment │  │• Writer     │ │
│  │ • mark_files_stale  │  │    目录管理      │  │• Compress   │ │
│  │ • get_usable_segs   │  │  • 缓存管理      │  │• JSON解析   │ │
│  └────────┬───────────┘  └────────┬─────────┘  └─────────────┘ │
│           │                       │                               │
│  ┌────────▼───────────────────────▼──────────────────────────┐  │
│  │                   IndexMaintainer                          │  │
│  │  • on_files_rewritten()    被动: 数据变更触发              │  │
│  │  • generate_rewrite_maps()  主动: 后台 Rewrite Map 生成    │  │
│  │  • rebuild_stale_indexes()  主动: 后台重建                 │  │
│  │  • garbage_collect()        主动: GC                      │  │
│  │  • validate_index()         查询: 校验索引完整性            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │              Iceberg Metadata 交互层                        │  │
│  │  • REST Catalog HTTP (libcurl)                             │  │
│  │  • 内嵌轻量 metadata JSON 读写 (降级方案)                   │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │              openGauss 集成                                  │  │
│  │  • bgworker 异步构建/维护                                    │  │
│  │  • ANALYZE hook 触发                                         │  │
│  │  • pg_catalog 系统表 (运行时缓存, SQL 可查)                  │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 3. Puffin 文件格式概要

### 3.1 文件布局

```
┌─────────────────────────────┐
│  Magic (4 bytes)   "PFA1"   │
├─────────────────────────────┤
│  Blob₁                      │  索引/统计的二进制数据
│  (变长, 可压缩)              │
├─────────────────────────────┤
│  ...                        │
├─────────────────────────────┤
│  Blobₙ                      │
├─────────────────────────────┤ ← Footer 开始
│  Magic (4B)                 │
├─────────────────────────────┤
│  FooterPayload (UTF-8 JSON) │  blobs[] + 文件级 properties
│  可选 LZ4 压缩               │
├─────────────────────────────┤
│  FooterPayloadSize (i32 LE) │
├─────────────────────────────┤
│  Flags (4B, bit0=LZ4)       │
├─────────────────────────────┤
│  Magic (4B)                 │
└─────────────────────────────┘
```

### 3.2 BlobMetadata 字段

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `type` | string | ✅ | Blob 类型标识符，自定义索引需定义唯一 type |
| `fields` | int[] | ✅ | Iceberg 字段 ID 列表 |
| `snapshot-id` | long | ✅ | 源快照 ID |
| `sequence-number` | long | ✅ | 源快照序列号 |
| `offset` | long | ✅ | Blob 在文件中的字节偏移 |
| `length` | long | ✅ | 压缩后字节长度 |
| `compression-codec` | string | ❌ | `"lz4"` / `"zstd"`，省略=不压缩 |
| `properties` | map | ❌ | 任意 key-value，索引参数和元数据放在此处 |

### 3.3 压缩

- FooterPayload: LZ4（可选，Flags bit0 标记）
- Blob: LZ4 或 Zstd（可选，`compression-codec` 标记）
- 整数编码：全部有符号小端序

### 3.4 TableMetadata.statistics[] 的存储策略

本模块**不将每个索引文件的条目直接放入** `TableMetadata.statistics[]`。改为：

- `statistics[]` 中只有**一条**指向 `index_catalog.puffin` 的指针
- 所有自定义索引的元数据集中在 `index_catalog.puffin` 内管理
- 内置 Puffin 统计（如 Theta Sketch）可以继续在 `statistics[]` 中独立存放
- catalog.puffin 文件名带版本号（`index_catalog_v{registry_version}.puffin`），旧版本随 Iceberg snapshot 过期自然清理

```
TableMetadata.statistics[]
  ├── [0]: type="apache-datasketches-theta-v1", path="...theta.puffin"  (内置)
  └── [1]: type="puffin.idx.registry-v1", path="...index_catalog_v17.puffin"
            ↑ 自定义索引从此入口发现
            properties: { "registry-version": "17", "registry-checksum": "sha256:..." }
```

### 3.5 statistics[] 与旧快照的关系

**关键事实**：Iceberg 的 `statistics[]` 是一个**累积数组**。它不会随 snapshot 过期而自动清理。

```
快照 S1: ANALYZE → statistics[] 新增条目
  [
    { "snapshot-id": 1001, "statistics-path": "idx_v1.puffin", ... }
  ]

快照 S3: 增量构建 → statistics[] 更新 (replace 模式)
  [
    { "snapshot-id": 1003, "statistics-path": "idx_v3.puffin", ... }
  ]
  ↑ 旧条目 (snapshot-id=1001) 被替换掉了

快照 S5: INSERT (无 ANALYZE) → statistics[] 不变
  同上

快照 S7: ANALYZE → 再次更新
  [
    { "snapshot-id": 1007, "statistics-path": "idx_v7.puffin", ... }
  ]
```

**完整方案**：每次 registry 更新时使用 **replace 模式**——替换 statistics[] 中的那一条 registry 指针，而不是追加新条目。因此 `statistics[]` 中永远只有 1 条自定义索引的条目，`snapshot-id` 反映了最后一次索引状态变更的快照。

**简易方案**：每次 rebuild 时，使用 **replace 模式**替换对应索引名称的 statistics[] 条目。如果每个索引独立更新（而不是批量更新），`statistics[]` 中的条目数 = 索引数量。

**旧快照的 TableMetadata**：每个 snapshot 对应的历史 `v{N}.metadata.json` 文件中，`statistics[]` 指向该快照时刻的 puffin 文件路径。这些历史 metadata 文件和它们引用的 puffin 文件一起，随 snapshot 过期被清理（见 §15）。

**查询时**：只使用当前 TableMetadata 中的 `statistics[]`，不访问历史版本。

---

## 4. 索引组织模型

### 4.1 核心概念

```
一个逻辑索引 (IndexGroup) = 1 个或多个物理索引段 (IndexSegment)

IndexGroup: 唯一标识一个逻辑索引
  - uuid: 全局唯一标识
  - name: 索引名称 (如 "btree_order_id")
  - type: 索引类型 (如 "puffin.idx.btree-v1")
  - field_id: 索引列

IndexSegment: 一个不可变的物理 Puffin 文件
  - uuid: 段唯一标识
  - puffin_path: 对应 .puffin 文件的路径
  - delta_from_segment: 增量链 (指向前一个 segment 的 uuid)
  - file_coverage: 覆盖哪些 Parquet 文件 (摘要)
  - status: 当前状态 (active/stale_partial/remapping/...)
```

### 4.2 增量链示例

```
IndexGroup "a1b2c3d4" (btree_order_id, field=1)
│
├── Segment[0] (uuid="s0-xxx", status=active)
│   puffin: snap-1001-2-idx1.puffin (16MB)
│   covers: files 0-499
│
├── Segment[1] (uuid="s1-yyy", status=active)
│   puffin: snap-1003-3-idx1delta.puffin (3.3MB)
│   delta_from_segment: "s0-xxx"   ← 增量, 基于 seg[0]
│   covers: files 500-999
│
└── Segment[2] (uuid="s2-zzz", status=active)
    puffin: snap-1011-6-idx1delta2.puffin (5MB)
    delta_from_segment: "s1-yyy"   ← 增量, 基于 seg[1]
    covers: files 1000-1499, spec_id=1

查询 field=1 时: 需搜索全部 3 个 segment 的 BTree
增量构建时: 只新增 Segment[3], 前 3 个 segment 的 Puffin 文件不动
```

### 4.3 文件拆分原则

- 一个 Puffin 文件 = 恰好一个 IndexSegment（大索引 >1MB 必须独立文件）
- 小统计索引（Theta + HLL <1MB）可合并，需满足相同覆盖范围 + 相同生命周期
- 增量构建只追加新 Puffin 文件，永不修改旧文件

---

## 5. 元数据设计：index_catalog.puffin

### 5.1 定位

`index_catalog.puffin` 是本模块的**元数据中枢**。它是一个标准 Puffin 文件，包含：

- **Blob₀ (registry-v1)**：索引注册表，描述所有 IndexGroup 和 IndexSegment 的目录、状态、文件覆盖摘要
- **Blob₁**：保留位，预留给未来的审计日志等扩展用途（当前版本不含此 Blob）

每次重写产生新文件（`index_catalog_v{registry_version}.puffin`），旧文件随 Iceberg snapshot 过期清理。

### 5.2 registry Blob 结构

```
┌──────────────────────────────────────────────────────────┐
│  Header:  magic, version, num_groups, registry_version,  │
│           updated_at_snapshot                            │
├──────────────────────────────────────────────────────────┤
│  Group Directory (每项变长):                              │
│    Group[0]: uuid, name, type, field_id, partition_spec, │
│              num_segments, segment_index_start            │
│    Group[1]: ...                                         │
├──────────────────────────────────────────────────────────┤
│  Segment List (每项变长):                                  │
│    Segment[0]: uuid, puffin_path, delta_from_seg_uuid,   │
│       deprecates_uuids, status, staleness_ratio,         │
│       built_at_snapshot, status_updated_at,              │
│       total_rows, num_pages, puffin_file_size,           │
│       file_coverage_format, file_coverage,               │
│       partition_summary                                  │
│    Segment[1]: ...                                       │
└──────────────────────────────────────────────────────────┘
```

### 5.3 关键字段说明

| 字段 | 位置 | 说明 |
|------|------|------|
| `registry_version` | Header | 单调递增计数器，每次状态变更 +1。用于缓存失效检测和版本化文件名 |
| `num_segments` | Group Directory | 该 Group 拥有的 Segment 数量；配合 `segment_index_start` 定位到 Segment List 中的起始位置 |
| `segment_uuid` | Segment List | 每个 segment 的唯一标识。被 `delta_from_seg_uuid` 和 `deprecates_uuids` 引用 |
| `delta_from_seg_uuid` | Segment List | 增量链：指向基础段的 uuid |
| `deprecates_uuids` | Segment List | 逗号分隔的旧 segment uuid 列表，表示新段取代了哪些旧段 |
| `file_coverage_format` | Segment List | 0=range 字符串（如 "0-499"），1=Roaring Bitmap。range 格式支持补充 stale 列表（"0-499,s:0,50"） |
| `status` | Segment List | 段状态（见 §6） |
| `status_updated_at` | Segment List | 状态最后变更时间（epoch ms），基本运维排查 |

### 5.4 索引覆盖了哪些 Parquet 文件、哪些分区 —— 记录与获取

#### 记录（构建时）

```
构建流程中:

Step 1: 从 Manifest 获取文件列表
  → 当前 snapshot 的所有 DataFile
  → 每个 DataFile: file_path, partition 值, record_count, 列统计

Step 2: 排序并分配 file_index
  → 按 file_path 字典序排序
  → 顺序分配 file_index = 0, 1, 2, ...
  → 构建 file_index_map_[]:
      [0] → { hash: XXH64("part-00001.parquet"),
               path: "created_at_day=2026-01-01/part-00001.parquet", rows: 2048 }
      [1] → { hash: XXH64("part-00002.parquet"),
               path: "created_at_day=2026-01-01/part-00002.parquet", rows: 2048 }
      ...
  → hash_to_index_ (运行时内存中构建):
      0xA3F2109B... → 0
      0x7E123456... → 1

Step 3: 记录分区覆盖 (如果有分区)
  → 从 Manifest 中每个文件的 partition 值
  → 按 partition_value 分组, 记录每组的 page_range
  → 序列化为 partition-bitmap-v1 Blob

Step 4: 写入 Puffin 文件
  PuffinWriter:
    add_blob(btree-v1, ...)              ← 索引数据
    add_blob(filebitmap-v1, ...)         ← 文件覆盖 Blob
    add_blob(partition-bitmap-v1, ...)   ← 分区覆盖 Blob (可选)
    finish()

  filebitmap-v1 Blob 内容:
    ┌────────────────────────────────────────┐
    │ magic: "FB"  version: 2                │
    │ num_files: 500                          │
    │ file_index_map_[0..499]:                │
    │   每项: path_hash(u64) + path_len(u16)  │
    │         + path_str(变长) + rows(i64)    │
    │ original_files: RoaringBitmap {0..499}  │
    └────────────────────────────────────────┘

Step 5: 在 registry 中记录摘要
  → segment.file_coverage_format = 0 (range)
  → segment.file_coverage = "0-499"
  → segment.partition_summary = "spec_id=0, range=2026-01-01~2026-01-10"
```

#### 获取（查询时）

```
发现阶段 (只需知道索引覆盖了哪些文件 — 够做索引选择):
  → 读 index_catalog.puffin registry (~10KB)
  → seg.file_coverage = "0-499"          ← 摘要, 不打开索引 .puffin
  → seg.partition_summary = "spec_id=0, range=01-01~01-10"
  → 到此已知覆盖范围 ✓

使用阶段 (需要做集合运算或回表 — 确认使用索引后才加载):
  → PuffinReader 打开对应 .puffin
  → 找到 filebitmap-v1 Blob → 加载:
      file_index_map_[]     ← file_index=3 → 哪个 Parquet 文件
      original_files        ← RoaringBitmap, 集合运算
      stale_files           ← 从 registry 加载后填充
  → 找到 partition-bitmap-v1 Blob → 加载:
      partition_directory   ← 查询分区 → 哪些 page_range
  → BTree.search(value, page_range=partition_directory.lookup(dt))

  RowAddress 中存的是 file_path_hash (u64):
    → hash_to_index_[hash] → file_index
    → file_index_map_[idx].file_path → 读 Parquet
    → stale_files.contains(file_index) → 过滤失效文件
```

#### 简易方案下的获取

```
发现阶段:
  → 遍历 statistics[] → 找到 field=1, status="active"
  → properties["file-list-hash"] 与当前文件列表 XXH64 比较
    → 一致 → 有效 ✓
    → 不一致 → 标记 invalid → 跳过

使用阶段: 同上, 打开 .puffin → 加载 filebitmap-v1 + partition-bitmap-v1 Blob
```

### 5.5 文件覆盖的完整映射

registry 中的 `file_coverage` 仅存摘要（如 `"0-499"` 或 `"0-499,s:0,50"`），用于索引发现。**完整映射**（file_index ↔ file_path_hash ↔ file_path）存储在各索引 `.puffin` 文件的 `filebitmap-v1` Blob 中。

- `file_index`：构建时对文件排序后从 0 顺序分配的稠密 u32，用于 RoaringBitmap 集合运算
- `file_path_hash`：XXH64(file_path)，用于 RowAddress 跨快照稳定寻址
- 索引 `.puffin` 内的 `filebitmap-v1` Blob 同时包含正反映射：`file_index_map_[]` 和 `hash_to_index_`

---

## 6. 索引状态机

### 6.1 状态定义

每个 **IndexSegment** 拥有独立的状态：

```
                    ┌─────────────┐
                    │  BUILDING   │  构建中, 查询不可用
                    └──────┬──────┘
                           │ 构建完成
                           ▼
                    ┌─────────────┐
         ┌──────────│   ACTIVE    │──────────┐
         │          └──────┬──────┘          │
         │                 │                  │
         │   文件变更        │   文件变更        │
         │   ratio<0.1      │   ratio>=0.1      │
         ▼                 ▼                  ▼
┌──────────────────┐  ┌──────────────┐  ┌──────────────┐
│  STALE_PARTIAL   │  │  REMAPPING   │  │STALE_ORPHAN  │
│ (跳过stale文件)   │  │ (Rewrite Map │  │ (等待Rebuild │
│ 查询可用 ✓        │  │  转译)       │  │ 或Rewrite Map│
│                  │  │ 查询可用 ✓    │  │ 查询不可用 ✗   │
└────────┬─────────┘  └──────┬───────┘  └──────┬───────┘
         │                   │                  │
         ▼                   ▼                  ▼
         REBUILDING     REBUILDING          REMAPPING
              │              │
              ▼              ▼
           ACTIVE         ACTIVE
         (新segment)    (旧→DEPRECATED)

    DEPRECATED → DELETED (GC)
```

### 6.2 状态速查

| 状态 | 查询可用? | 说明 |
|------|----------|------|
| `building` | ❌ | 构建中，数据不完整 |
| `active` | ✅ | staleness_ratio = 0 |
| `stale_partial` | ✅ | ratio < 0.1，跳过少量 stale 文件即可 |
| `remapping` | ✅ | 有 Rewrite Map，查询时自动转译行地址 |
| `stale_orphan` | ❌ | ratio ≥ 0.3 或无 Rewrite Map，等待重建 |
| `rebuilding` | ❌ | 正在重建 |
| `deprecated` | ❌ | 已被新 segment 替代 |
| `deleted` | ❌ | 可 GC |

### 6.3 状态存储

- **权威源**：`index_catalog.puffin` 的 registry Blob
- **运行时缓存**：openGauss pg_catalog 系统表（规划器直接查内存）
- **交叉验证**：各索引 `.puffin` Footer 中也可存 status（但可能过时）
- 状态变更**只重写** `index_catalog.puffin`（~10KB）并更新系统表，不重写索引 `.puffin` 文件

---

## 7. Iceberg 元数据交互

### 7.1 交互方式

本模块不通过 JNI 调用 Java SDK。改为以下两种方式之一（可共存）：

| 方式 | 适用场景 | 说明 |
|------|---------|------|
| **REST Catalog HTTP API** | 生产环境 | 通过 Iceberg REST Catalog 规范读写 TableMetadata、获取 Manifest。openGauss 内通过 libcurl 调用 |
| **内嵌轻量 metadata 读写** | 无 Catalog 服务的场景 | 直接从对象存储（S3/HDFS/本地）读取 v{N}.metadata.json，写入新版本，原子更新 version-hint.text。几百行 C 代码即可 |

### 7.2 需要的能力

| 能力 | 用途 | 调用时机 |
|------|------|---------|
| 读取 TableMetadata | 获取 snapshots、statistics[]、schemas、partition-specs | 查询规划、索引构建、维护 |
| 写入/更新 TableMetadata | 提交 catalog.puffin 注册或状态变更 | 构建完成、状态变更 commit |
| 获取 DataFile 列表 | 获取 snapshot 下的 Parquet 文件及列统计 | 索引构建、查询规划 |
| 比较 Snapshot 差异 | 获取两个 snapshot 间的新增/删除文件 | 增量构建差异检测 |
| 读取 Manifest | 获取当前 snapshot 的活跃文件集合 | 查询规划、stale 检测 |

### 7.3 不自己实现的能力

| 不自己实现 | 原因 |
|-----------|------|
| Puffin 文件读写 | 本模块已有 C 实现 |
| Blob 序列化/反序列化 | 各 IIndexPlugin 自行管理 |
| 索引具体数据结构 | 插件边界之外 |

---

## 8. 核心数据结构

### 8.1 索引目录

```c
/* ====== 索引组 ====== */
typedef struct IndexGroup {
    char        uuid[37];            /* UUID 字符串 */
    char       *name;                /* 索引名称, malloc'd */
    char       *blob_type;           /* 索引类型, malloc'd */
    int32_t     field_id;            /* Iceberg 字段 ID */
    int32_t     partition_spec_id;   /* 分区 spec */
    int64_t     created_at_snapshot;

    uint16_t    num_segments;        /* 该组拥有的 segment 数 */
    uint32_t    segment_index_start; /* Segment List 中的起始索引 */

    /* 运行时: 已解析的 segment 指针数组 (非持久化) */
    struct IndexSegment **segments;  /* calloc'd, 长度 num_segments */
} IndexGroup;

/* ====== 索引段 ====== */
typedef struct IndexSegment {
    char        uuid[37];            /* 段 UUID */
    char        group_uuid[37];      /* 所属 IndexGroup UUID */
    char       *puffin_path;        /* .puffin 文件路径, malloc'd */
    char        delta_from_uuid[37]; /* delta-from 链 (全零=基础段) */
    char       *deprecates_uuids;   /* 逗号分隔的旧段 UUID, malloc'd (可为 NULL) */

    /* 状态 */
    uint8_t     status;             /* 见 PuffinSegmentStatus 枚举 */
    float       staleness_ratio;
    int64_t     built_at_snapshot;
    int64_t     status_updated_at;

    /* 构建统计 */
    int64_t     total_rows;
    int32_t     num_pages;
    int64_t     puffin_file_size;
    int32_t     puffin_footer_size;

    /* 文件覆盖摘要 */
    uint8_t     file_coverage_format; /* 0=range, 1=bitmap */
    char       *file_coverage;       /* "0-499" 或 "0-499,s:0,50" 或 RoaringBitmap */
    char       *partition_summary;   /* 如 "spec_id=0,range=01-01~01-10", malloc'd */

    /* 文件覆盖详情 (延迟加载, 从 .puffin 的 filebitmap Blob) */
    bool        file_map_loaded;
    struct FileMapEntry *file_index_map;    /* 数组, 长度 = original_files 基数 */
    struct RoaringBitmap *original_files;   /* file_index 集合 */
    struct RoaringBitmap *stale_files;      /* 失效 file_index 子集 */
    /* hash_to_index_ 在加载时构建为内存哈希表 */

    /* Rewrite Map (生效时) */
    char       *rewrite_map_path;    /* Rewrite Map .puffin 路径, 可为 NULL */
} IndexSegment;

/* ====== 文件映射条目 ====== */
typedef struct FileMapEntry {
    uint64_t    path_hash;           /* XXH64(file_path) */
    char       *file_path;           /* 文件相对路径, malloc'd */
    int64_t     row_count;           /* 文件行数 */
} FileMapEntry;

/* ====== 段状态枚举 ====== */
typedef enum {
    SEG_STATUS_BUILDING       = 0,
    SEG_STATUS_ACTIVE         = 1,
    SEG_STATUS_STALE_PARTIAL  = 2,
    SEG_STATUS_REMAPPING      = 3,
    SEG_STATUS_STALE_ORPHAN   = 4,
    SEG_STATUS_REBUILDING     = 5,
    SEG_STATUS_DEPRECATED     = 6,
    SEG_STATUS_DELETED        = 7,
} PuffinSegmentStatus;
```

### 8.2 行地址

```c
/* ====== 统一行地址 (128-bit) ====== */
typedef struct RowAddress {
    uint64_t high;   /* [file_path_hash:48 | rg_index:16] */
    uint64_t low;    /* [row_in_rg:16 | reserved:48] */
} RowAddress;

/* 构造: file_hash=XXH64(path), rg_index, row_in_rg */
RowAddress puffin_rowaddr_make(uint64_t file_hash, uint16_t rg, uint16_t row);

/* 解码 */
uint64_t puffin_rowaddr_file_hash(RowAddress addr);
uint16_t puffin_rowaddr_rg_index(RowAddress addr);
uint16_t puffin_rowaddr_row_in_rg(RowAddress addr);
```

### 8.3 索引评估结果

```c
typedef enum {
    INDEX_CANNOT_DECIDE = 0,   /* 无法判断 */
    INDEX_CAN_SKIP      = 1,   /* 确定无匹配 → 跳过文件 */
    INDEX_CAN_MATCH     = 2,   /* 可能有匹配 → 需读取 */
    INDEX_EXACT_MATCH   = 3,   /* 精确行定位 */
} IndexVerdict;

typedef struct IndexEvalResult {
    IndexVerdict verdict;
    char        *reason;            /* 可读的原因描述, malloc'd */

    /* INDEX_EXACT_MATCH 时有效 */
    RowAddress  *exact_rows;        /* 精确行地址数组, malloc'd */
    size_t       exact_row_count;

    /* 可选: 全文/向量索引的带分数结果 */
    struct {
        RowAddress *rows;
        float      *scores;
        size_t      count;
    } ranked_rows;
} IndexEvalResult;

/* 行过滤约束 (限制搜索范围) */
typedef struct RowFilter {
    struct RoaringBitmap *allowed_file_indices;  /* file_index 集合 (稠密 u32) */
} RowFilter;
```

### 8.4 索引可用性

```c
typedef enum {
    IDX_USABILITY_SAFE_TO_USE         = 0,
    IDX_USABILITY_SAFE_WITH_STALE_SKIP = 1,
    IDX_USABILITY_USE_WITH_REMAP      = 2,
    IDX_USABILITY_SKIP                = 3,
} IndexUsability;

typedef struct UsabilityResult {
    IndexUsability usability;
    char          *reason;
    double         staleness_ratio;
    struct RewriteMap *rewrite_map;   /* IDX_USABILITY_USE_WITH_REMAP 时有效 */
} UsabilityResult;
```

### 8.5 构建配置

```c
typedef struct BuildConfig {
    /* 通用参数 */
    int32_t     batch_size;          /* 构建批量行数, 默认 65536 */
    uint8_t     compression;         /* 0=none, 1=LZ4, 2=Zstd */
    int32_t     compression_level;   /* Zstd: 1-22 */
    bool        compress_footer;
    int32_t     num_build_threads;   /* 0=auto */

    /* 阈值 */
    float       staleness_remap_threshold;    /* 默认 0.1 */
    float       staleness_rebuild_threshold;  /* 默认 0.3 */
    int32_t     defragment_chain_length;      /* 默认 10 */

    /* 每索引类型自定义配置 (key-value, 由各插件自行解析) */
    int32_t     per_type_config_count;
    char      **per_type_config_keys;
    char      **per_type_config_values;
    /* 示例: key="btree.page_size", value="4096"          */
    /*       key="bloom.fpp", value="0.01"                */
    /*       key="hnsw.M", value="16"                     */
} BuildConfig;
```

### 8.6 Puffin 读写类型

```c
/* ====== Blob 元数据 ====== */
typedef struct PuffinBlobMeta {
    char       *type;               /* blob 类型, malloc'd */
    int32_t    *fields;             /* 字段 ID 数组, malloc'd */
    size_t      fields_count;
    int64_t     snapshot_id;
    int64_t     sequence_number;
    int64_t     offset;
    int64_t     length;
    uint8_t     compression;        /* 0=none, 1=LZ4, 2=Zstd */
    int32_t     properties_count;
    char      **prop_keys;          /* malloc'd */
    char      **prop_values;        /* malloc'd */
} PuffinBlobMeta;

/* ====== Blob 数据 ====== */
typedef struct PuffinBlob {
    PuffinBlobMeta  meta;
    uint8_t        *data;           /* 已解压, malloc'd */
    size_t          data_len;
} PuffinBlob;

/* ====== 文件元数据 (Footer) ====== */
typedef struct PuffinFileMeta {
    int32_t          footer_version;
    PuffinBlobMeta  *blobs;         /* malloc'd 数组 */
    size_t           blob_count;
    int32_t          prop_count;
    char           **prop_keys;
    char           **prop_values;
    int64_t          footer_offset;
} PuffinFileMeta;

/* ====== Blob 写入描述符 ====== */
typedef struct PuffinBlobDesc {
    const char     *type;
    const int32_t  *fields;
    size_t          fields_count;
    int64_t         snapshot_id;
    int64_t         sequence_number;
    uint8_t         compression;     /* 0=none, 1=LZ4, 2=Zstd */
    int32_t         prop_count;
    const char    **prop_keys;
    const char    **prop_values;
} PuffinBlobDesc;
```

---

## 9. 索引管理接口（C ABI）

本模块对外暴露 C ABI，供 openGauss 内核的其他子系统（规划器、执行器、运维命令）调用。

### 9.1 索引发现

```c
/* ====== IndexCatalog ====== */

/* 从 index_catalog.puffin 加载全部索引目录。
 * catalog_path: catalog.puffin 的路径
 * 返回 group 数组和数量, *out_groups 由调用方通过 puffin_catalog_free() 释放 */
int puffin_catalog_load(const char              *catalog_path,
                         IndexGroup           ***out_groups,
                         size_t                 *out_group_count);

/* 刷新: 检查 registry_version 是否变更, 未变更则复用缓存。
 * table_path: Iceberg 表路径 (如 "s3://bucket/db/orders") */
int puffin_catalog_refresh(const char *table_path);

/* 按 field_id 查找匹配的索引组。
 * catalog 必须已经通过 puffin_catalog_load() 或 _refresh() 加载。
 * 返回匹配的 group 数组, *out_count 为数量。内存由 catalog 内部管理, 不可 free */
int puffin_catalog_find_groups(int32_t           field_id,
                                IndexGroup      **out_groups,
                                size_t           *out_count);

/* 释放 catalog 内存 */
void puffin_catalog_free(IndexGroup **groups, size_t count);

/* 按 uuid 查找 segment。需要先加载 catalog。
 * 返回 segment 指针 (内部管理, 不可 free), 未找到返回 NULL */
IndexSegment* puffin_catalog_get_segment(const char *seg_uuid);
```

### 9.2 构建管理

```c
/* ====== IndexSegmentManager ====== */

/* 创建索引组。返回 group_uuid (调用方 free) */
int puffin_segment_mgr_create_group(int32_t       field_id,
                                     const char   *index_type,
                                     const char   *index_name,
                                     char         *out_uuid_buf,
                                     size_t        uuid_buf_len);

/* 全量构建: 为当前快照所有文件构建一个新 segment。
 * 内部: 通过 Iceberg metadata 交互层获取 DataFile 列表 → 调用 IIndexPlugin 构建 → 写 Puffin。
 * 阻塞直到构建完成。返回新 segment_uuid */
int puffin_segment_mgr_build_full(const char *group_uuid,
                                   const char *table_path,
                                   char       *out_seg_uuid,
                                   size_t      uuid_buf_len);

/* 增量构建: 仅为 from_snapshot → current 之间的新增文件构建 delta segment。
 * 如果无新文件, *out_seg_uuid 为空字符串, 返回码仍为 0。
 * 自动处理 removed_files (标记部分失效) */
int puffin_segment_mgr_build_incremental(const char *group_uuid,
                                          const char *table_path,
                                          int64_t     from_snapshot,
                                          char       *out_seg_uuid,
                                          size_t      uuid_buf_len);

/* 标记文件失效 (CoW UPDATE / Compaction 后调用)。
 * rewritten_paths: 被重写的文件路径列表
 * new_paths: 重写后的新文件路径列表 (同长度) */
int puffin_segment_mgr_mark_stale(const char    *table_path,
                                   const char   **rewritten_paths,
                                   const char   **new_paths,
                                   size_t         count);

/* 收集指定 field 当前可用的 segment 列表。
 * 自动检查每个 segment 的可用性 (调用 IndexUsabilityChecker)。
 * *out_segments 和 *out_usabilities 长度相同, 调用方通过 puffin_segment_mgr_free_result() 释放 */
int puffin_segment_mgr_get_usable(int32_t               field_id,
                                   int64_t               snapshot_id,
                                   IndexSegment        **out_segments,
                                   UsabilityResult      **out_usabilities,
                                   size_t               *out_count);
void puffin_segment_mgr_free_result(IndexSegment     *segments,
                                     UsabilityResult  *usabilities,
                                     size_t            count);
```

### 9.3 安全校验

```c
/* 校验 segment 对当前查询的安全性。
 * active_file_indices: 当前 snapshot 中实际存在的 file_index 集合 (RoaringBitmap) */
UsabilityResult puffin_check_usability(const IndexSegment    *seg,
                                        const RoaringBitmap  *active_file_indices);
```

### 9.4 Puffin 文件读写

```c
/* ====== PuffinReader ====== */
typedef struct PuffinReader PuffinReader;

PuffinReader* puffin_reader_open(const char *file_path);
void          puffin_reader_close(PuffinReader *reader);
int           puffin_reader_read_footer(PuffinReader *reader, PuffinFileMeta *out_meta);
int           puffin_reader_read_blob(PuffinReader *reader,
                                       const PuffinBlobMeta *meta, PuffinBlob *out_blob);
int           puffin_reader_find_blob(PuffinReader *reader, const char *type,
                                       PuffinBlob *out_blob);
void          puffin_blob_free(PuffinBlob *blob);
void          puffin_file_meta_free(PuffinFileMeta *meta);

/* ====== PuffinWriter ====== */
typedef struct PuffinWriter PuffinWriter;

PuffinWriter* puffin_writer_open(const char *file_path, bool compress_footer);
int           puffin_writer_add_blob(PuffinWriter *writer,
                                      const PuffinBlobDesc *desc,
                                      const uint8_t *data, size_t data_len);
int           puffin_writer_finish(PuffinWriter *writer);
void          puffin_writer_abort(PuffinWriter *writer);
int64_t       puffin_writer_bytes_written(PuffinWriter *writer);
int64_t       puffin_writer_footer_size(PuffinWriter *writer);
```

---

## 10. 索引实现接口（插件框架）

### 10.1 索引插件 vtable

各具体索引（BTree、Bloom、Bitmap、IVF 等）通过 C 函数指针表（vtable pattern）实现：

```c
typedef struct IIndexPlugin IIndexPlugin;

typedef struct {
    /* 标识 */
    const char* (*blob_type)(void);

    /* 序列化 (Puffin blob ↔ 内存对象) */
    int  (*deserialize)(IIndexPlugin *idx, const uint8_t *data, size_t len);
    int  (*serialize)(const IIndexPlugin *idx, uint8_t **out_data, size_t *out_len);

    /* 构建 */
    void (*begin_build)(IIndexPlugin *idx);
    void (*add_row)(IIndexPlugin *idx, const IcebergValue *value,
                    RowAddress row_addr);
    void (*end_build)(IIndexPlugin *idx);
    bool (*merge)(IIndexPlugin *idx, const IIndexPlugin *other);

    /* 查询 */
    IndexEvalResult (*evaluate)(const IIndexPlugin *idx,
                                 const Expression *expr,
                                 const RowFilter *filter);

    /* 配置 (从 BuildConfig.per_type_config 解析) */
    void (*configure)(IIndexPlugin *idx, int n_keys,
                       const char **keys, const char **values);

    /* 元数据 */
    size_t  (*memory_bytes)(const IIndexPlugin *idx);
    int64_t (*total_rows)(const IIndexPlugin *idx);
    size_t  (*estimated_serialized_bytes)(const IIndexPlugin *idx);

    /* 销毁 */
    void (*destroy)(IIndexPlugin *idx);
} IIndexPluginVtable;

struct IIndexPlugin {
    const IIndexPluginVtable *vtable;
    void                     *internal;   /* 索引实现私有状态 */
};
```

### 10.2 索引注册

```c
/* 工厂函数类型 */
typedef IIndexPlugin* (*PuffinIndexFactory)(void);

/* 注册索引工厂 (按 blob_type) */
void puffin_index_registry_register(const char         *blob_type,
                                     PuffinIndexFactory  factory,
                                     int                 priority);

/* 按 type 创建索引实例 */
IIndexPlugin* puffin_index_registry_create(const char *blob_type);
```

### 10.3 索引实现方的交付物

每个自定义索引类型需要提供：

1. 唯一的 `blob_type` 字符串（如 `"puffin.idx.btree-v1"`）
2. `IIndexPluginVtable` 完整实现
3. 工厂函数 + 注册调用
4. Blob 序列化格式规范（供跨语言读取）
5. （可选）`configure()` 支持的 key 列表及含义

### 10.4 已有 Blob 类型

| Blob Type | 粒度 | 用途 |
|-----------|------|------|
| `puffin.idx.btree-v1` | Row Group 级 | 范围/等值查询 + 行定位 |
| `puffin.idx.bloom-v1` | 文件组级 | 等值/IN 快速排除 |
| `puffin.idx.bitmap-v1` | 行级 | 低基数列精确行定位 |
| `puffin.idx.filebitmap-v1` | — | 文件覆盖位图 + file_index 映射 |
| `puffin.idx.partition-bitmap-v1` | — | 分区值 → Page 范围映射 |
| `puffin.idx.rewritemap-v1` | — | 文件重映射（old_addr → new_addr） |
| `puffin.idx.registry-v1` | — | 索引注册表（中枢） |

---

## 11. 索引维护接口

### 11.1 被动维护（数据变更触发）

```c
/* CoW UPDATE / Compaction 回调 — 同步调用, 在事务提交前执行 */
int puffin_maintainer_on_rewrite(const char  *table_path,
                                  const char **old_file_paths,
                                  const char **new_file_paths,
                                  size_t       count);

/* INSERT 回调 — 通常无操作 (新文件在下一次 ANALYZE 时处理) */
int puffin_maintainer_on_append(const char  *table_path,
                                 const char **new_file_paths,
                                 size_t       count);

/* MoR DELETE 回调 — 无操作 (delete file 在引擎读取层处理, 索引无需感知) */
int puffin_maintainer_on_delete(const char  *table_path,
                                 const char **delete_file_paths,
                                 size_t       count);
```

### 11.2 主动维护（bgworker 异步任务）

```c
/* 生成 Rewrite Map — 对 stale_orphan 状态的 segment 异步生成 */
int puffin_maintainer_generate_rewrite_maps(const char *table_path);

/* 重建 stale segment — staleness_ratio >= 0.3 触发 */
int puffin_maintainer_rebuild_stale(const char *table_path);

/* 合并过于分散的增量链 — delta-from 链长度 >= 10 触发 */
int puffin_maintainer_defragment(const char *group_uuid, const char *table_path);

/* 垃圾回收 — 清理 deprecated 超过 TTL 的 segment */
int puffin_maintainer_gc(const char *table_path);

/* 删除整个索引组 */
int puffin_maintainer_drop_group(const char *group_uuid, const char *table_path);

/* 校验索引完整性, 返回报告 JSON */
int puffin_maintainer_validate(const char *group_uuid,
                                const char *table_path,
                                char      **out_report_json);
```

### 11.3 维护操作触发条件

| 操作 | 触发条件 | 同步/异步 |
|------|---------|----------|
| mark_files_stale | CoW UPDATE / Compaction commit | 同步 |
| generate_rewrite_maps | staleness_ratio ≥ 0.1 | 异步 (bgworker) |
| rebuild_stale | staleness_ratio ≥ 0.3 | 异步 (bgworker) |
| defragment | delta-from 链长度 ≥ 10 | 异步 (低优先级) |
| garbage_collect | deprecated 超过 TTL | 异步 (低优先级) |
| validate | 运维主动调用 | 同步 |

### 11.4 MoR DELETE 特别说明

Merge-on-Read DELETE **不触发任何索引维护**。MoR DELETE 只写 delete file，不重写 DataFile。索引返回的 row_addr 在 openGauss 引擎读取 Parquet 数据时，由 Iceberg 读取层检查 delete file 并过滤已删除行——索引无需感知。

---

## 12. openGauss 集成点

### 12.1 bgworker 异步任务

openGauss 的 background worker 框架用于执行所有异步索引操作：

```c
/* 注册 bgworker — 在 openGauss 启动时调用 */
void puffin_bgworker_init(void);

/* bgworker 内部循环:
 *   1. 从任务队列取任务 (build / rewrite_map / rebuild / defragment / GC)
 *   2. 执行任务
 *   3. 更新 index_catalog.puffin + 系统表
 *   4. 等待下一任务或超时休眠
 */
```

build_full/build_incremental 可以选择**同步**（用户 ANALYZE 等待）或**异步**（bgworker 执行，ANALYZE 立即返回，查询规划器通过 status=building 知晓不可用）。

### 12.2 ANALYZE hook

```c
/* openGauss ANALYZE 命令的 hook。
 * 当用户执行 ANALYZE tablename 时触发索引构建调度。
 * 如果表是 Iceberg 外部表 → 调用本模块的构建接口 */
void puffin_analyze_hook(Oid table_oid, const char *table_path,
                          const int32_t *column_ids, int n_columns);
```

### 12.3 pg_catalog 系统表

在 openGauss 的 pg_catalog 中增加一张系统表，作为 registry 的**运行时缓存**：

```sql
-- 系统表定义 (概念, 具体名字待定)
CREATE TABLE pg_catalog.pg_puffin_index (
    group_uuid       text PRIMARY KEY,
    index_name       text,
    index_type       text,
    field_id         integer,
    segment_uuid     text,
    segment_status   smallint,     -- 0=BUILDING, 1=ACTIVE, ...
    staleness_ratio  real,
    puffin_path      text,
    file_coverage    text,         -- 摘要
    built_at_snap    bigint,
    updated_at       timestamptz
);
```

- **写入时机**：每次 index_catalog.puffin 更新后同步刷新系统表
- **读取方式**：规划器直接查询系统表（内存 O(1) 查找），无需解析 Puffin 文件
- **恢复方式**：系统表损坏/丢失时从 index_catalog.puffin 重建
- **SQL 可查**：运维人员可通过 `SELECT * FROM pg_puffin_index` 查看索引状态

### 12.4 崩溃恢复

本模块**不写 WAL**。catalog.puffin 本身是 Iceberg snapshot 原子 commit 的产物——只要 TableMetadata 更新成功了，catalog.puffin 就完整写入了。openGauss crash 后：

1. 从当前 snapshot 对应的 TableMetadata 找到 catalog.puffin 路径
2. 加载 registry → 恢复到 crash 前的一致状态
3. bgworker 重新启动，检查是否有 building 状态的 segment → 如有，重新构建

---

## 13. 查询流程

### 13.1 查询时索引使用流程

```
查询: SELECT * FROM t WHERE col = value

1. 分区裁剪 (Manifest List) → 保留相关 Manifest
2. 文件裁剪 (Manifest Entry stats) → 保留候选文件
3. 索引发现:
   → 查 pg_catalog.pg_puffin_index (内存, O(1))
   → 若系统表未加载 → 读 TableMetadata.statistics[]
   → 找到 index_catalog.puffin → 加载 registry → 更新系统表
4. 安全校验:
   → 对每个 segment, puffin_check_usability(seg, active_files)
   → SKIP 的 segment 回退全扫描
5. 索引搜索:
   → 对可用 segment, 加载对应 .puffin 中的索引 Blob
   → 调用 vtable->evaluate(expr)
6. 结果验证:
   → SAFE_WITH_STALE_SKIP: hash→file_index→stale_files.contains() → 过滤
   → USE_WITH_REMAP: Rewrite Map 转译行地址
7. 回表:
   → RowAddress → file_path_hash → Parquet 文件 → Page 定位 → 读数据
8. 合并: 索引命中 ∪ 全扫描回退 → 去重 → 返回
```

### 13.2 索引缓存

- **元数据层**：pg_catalog 系统表 + registry_version 比较（版本不变 = 不重加载）
- **数据层**：LRU 缓存已反序列化的 IIndexPlugin 对象（最大 128MB / 1000 条目）

---

## 14. 文件覆盖与部分失效

### 14.1 两层文件标识

| | file_index (u32) | file_path_hash (u64) |
|---|---|---|
| 作用域 | segment 内部 | 跨 segment / 跨 snapshot |
| 产生方式 | 构建时排序后顺序分配 | XXH64(file_path) |
| 用途 | RoaringBitmap 集合运算 | RowAddress 寻址 |
| 稳定性 | 同 segment 内稳定 | 跨快照永久稳定 |

### 14.2 部分失效流程

```
CoW UPDATE 重写 part-00001.parquet → part-01000.parquet

1. 文件路径 → XXH64 → hash
2. seg->file_index_map_ 中查找 hash → file_index = 0
3. seg->stale_files->add(0)          ← RoaringBitmap 操作
4. staleness_ratio = 1/500 = 0.002  (< 0.1 → stale_partial)
5. 更新 index_catalog.puffin registry + pg_puffin_index:
   segment.status = "stale_partial"
   file_coverage = "0-499,s:0"       ← range+stale 格式
6. 查询时: BTree 返回 row_addr → hash→file_index→stale_files.contains()
   → stale 行丢弃, 其余正常使用
```

### 14.3 Rewrite Map

当 staleness 超过阈值（≥ 0.1）时，bgworker 异步生成 Rewrite Map。它是一个独立的 `.puffin` 文件，记录 `old_file_path_hash → new_file_path_hash` 的行范围映射。查询时 BTree 返回的旧地址通过 Rewrite Map 转译为当前快照中的有效地址。

---

## 15. Puffin 文件生命周期与旧文件清理

### 15.1 完整方案下的文件产生与过时模式

```
Snapshot S1: ANALYZE → build_full → snap-1001-2-idx1.puffin (Segment[0])
  → index_catalog_v1.puffin (registry_version=1)
  → statistics[] 指向: index_catalog_v1.puffin

Snapshot S3: INSERT + incremental build
  → snap-1003-3-idx1delta.puffin (Segment[1], delta-from Segment[0])
  → index_catalog_v3.puffin (registry_version=3) ← 新版本
  → statistics[] 指向: index_catalog_v3.puffin
  → index_catalog_v1.puffin ← 孤儿 (不再被任何 snapshot 引用)

Snapshot S7: CoW UPDATE → mark stale_partial → bgworker generate_rewrite_map
  → snap-1007-4-rewrite.puffin (Rewrite Map)
  → index_catalog_v5.puffin (更新 segment 状态)
  → index_catalog_v3.puffin ← 孤儿

Snapshot S15: bgworker → rebuild
  → snap-1015-8-idx1_rebuilt.puffin (新 Segment, deprecates 旧 Segment[0]+[1])
  → index_catalog_v8.puffin
  → Segment[0] 和 Segment[1] 的 .puffin ← 孤儿 (status=deprecated → deleted)
  → snap-1007-4-rewrite.puffin ← 孤儿 (Rewrite Map 不再需要)

Snapshot S30: GC
  → 清理: index_catalog_v1~v7.puffin (旧 registry)
         snap-1001-2-idx1.puffin (旧 Segment[0])
         snap-1003-3-idx1delta.puffin (旧 Segment[1])
         snap-1007-4-rewrite.puffin (旧 Rewrite Map)
  → 保留: index_catalog_v8.puffin + 活跃 segment 的 .puffin
```

### 15.2 两类文件的清理策略

#### A. index_catalog.puffin 版本清理

```
catalog.puffin 版本命名: index_catalog_v{registry_version}.puffin

每次重写产生新文件, 旧文件不再被 statistics[] 引用。
清理条件: 产生该 catalog 版本的 snapshot 已过期 (超过 snapshot TTL)。

示例:
  index_catalog_v1.puffin 在 snapshot S1 产生
  index_catalog_v3.puffin 在 snapshot S3 产生
  当前 snapshot = S30, snapshot TTL = 7 个

  如果 S1 已过期 (30-1 > 7) → index_catalog_v1.puffin 可删除
  如果 S3 未过期                     → index_catalog_v3.puffin 保留

  注意: 始终保留当前 snapshot 引用的最新版本。
```

#### B. Segment / Rewrite Map 的 Puffin 文件清理

```
清理条件 (全部满足):
  1. segment.status = "deleted" (已走完 deprecated → deleted 状态流转)
  2. 该 segment 不被任何活跃 segment 的 delta_from_seg_uuid 引用
  3. 该 segment 不被任何活跃 segment 的 deprecates_uuids 引用
  4. 产生该 segment 的 snapshot 已过期 (TTL + 安全窗口)

安全窗口 (recommended): 3× snapshot TTL
  → 确保所有在过期 snapshot 上的活跃查询已结束
  → 在 openGauss 场景下, 查询不可能跨 snapshot (每次查询绑定一个 snapshot)

Rewrite Map 清理:
  → 其服务的所有 segment 已被 deleted
  → 或 segment 已被 rebuilt (新的 segment 不依赖旧 Rewrite Map)
```

### 15.3 孤儿文件检测

```
方案: 定期扫描 metadata/ 目录, 与 registry 交叉验证

  1. 读 TableMetadata.statistics[] → 获取当前 index_catalog.puffin 路径
  2. 加载当前 registry → 收集所有活跃 segment 和 Rewrite Map 的路径
     活跃路径 = {
       current_catalog_path,
       for each segment where status != deleted: segment.puffin_path,
       for each Rewrite Map serving active segments: rewrite_map_path
     }
  3. 列出 metadata/ 下的 *.puffin 文件
  4. 孤儿 = 磁盘文件 - 活跃路径
  5. 对每个孤儿, 检查其产生 snapshot 是否已过期 → 是 → 删除

  在 openGauss 中可作为一个 bgworker 周期任务 (如每小时一次)。
```

### 15.4 Rebuild 时的原子性

```
与简易方案一致的原则: 先写新文件, 再更新指针, 最后删旧文件

完整方案 rebuild 时序:
  1. 构建新 segment → 写入新 Puffin 文件
  2. 写入新的 index_catalog.puffin (registry_version++):
     - 新 segment: status="active", deprecates_uuids=旧段 UUID 列表
     - 旧 segment: status="deprecated"
  3. 更新 TableMetadata.statistics[] → 指向新 catalog.puffin
  4. commit TableMetadata
  5. (异步) bgworker GC: 检查旧 segment → 标记 deleted → 物理删除 Puffin 文件

  如果在步骤 2-3 之间 crash:
    → 新 segment 的 Puffin + 新 catalog.puffin 都已写入
    → TableMetadata 未更新 → 仍然指向旧 catalog.puffin
    → 查询用旧 registry → 旧 segment 仍为 active → 不受影响 ✓
    → 新文件是孤儿 → 后续 GC 清理
```

### 15.5 与 Iceberg Snapshot 生命周期的绑定

本模块的文件清理**不独立维护 TTL**，而是**依赖 Iceberg 的 snapshot 过期机制**：

```
Iceberg snapshot 过期 → 
  1. 过期 snapshot 的 metadata JSON 文件被清理
  2. 过期 snapshot 的 Manifest List / Manifest File 被清理
  3. 过期 snapshot 独占的 DataFile 被清理
  4. (本模块) 产生于过期 snapshot 的旧 catalog.puffin 和旧 segment .puffin 被清理

实现方式:
  - 如果使用 Iceberg REST Catalog: 监听 snapshot 过期事件 (webhook / 定期轮询)
  - 如果使用内嵌 metadata 读写: 在 openGauss 中维护 own_snapshot_id 字段,
    每个 Puffin 文件记录自己是在哪个 snapshot 产生的,
    GC 时比较 snapshot TTL

  关键: 每次 GC 后, 只有当前 snapshot 引用的 Puffin 文件保留,
        其余随 Iceberg 正常 snapshot 过期流程清理。
```

---

## 16. 附录：目录结构

```
s3://bucket/warehouse/db/orders/metadata/
├── v1.metadata.json                            ← TableMetadata
├── index_catalog_v17.puffin                     ← 索引注册表 (中枢, ~10KB)
│     Blob₀: registry-v1 (所有 group/segment)
│     Blob₁: 保留
│
├── snap-1001-1-abc.avro                        ← Manifest List
├── m1-xxxx.avro                                ← Manifest File
│
├── snap-1001-2-idx1.puffin                     ← Index-A Segment[0] (16MB)
├── snap-1003-3-idx2.puffin                     ← Index-B Segment (32MB)
├── snap-1003-3-idx1delta.puffin                ← Index-A Segment[1] (3.3MB)
├── snap-1011-6-idx1delta2.puffin               ← Index-A Segment[2] (5MB)
├── snap-1007-4-rewrite.puffin                  ← Rewrite Map (~1KB)
└── snap-1015-7-ivf.puffin                      ← IVF 向量索引 (150MB)

data/
├── created_at_day=2026-01-01/
│   ├── part-00001.parquet
│   └── ...
└── ...
```
