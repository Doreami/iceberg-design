# Iceberg 湖仓索引开发实践（AI 辅助开发项目）— PPT 大纲

> 用途：领导/管理层汇报 · 建议 17~18 页，单页一个主题
> 数据口径与《Iceberg湖仓索引AI辅助开发实践汇报.md》一致
> 汇报对象：openGauss 数据湖索引能力栈（索引引擎 + Rust 桥接 + FDW + Catalog 全部代码）

---

## 封面
- 标题：Iceberg 湖仓索引开发实践——AI 辅助开发项目汇报
- 副标题：为 openGauss 构建完整的数据湖索引能力栈
- 周期：2026-06 底 — 2026-08 · 汇报人 / 部门

---

## P1 项目定位（1 页讲清"做了什么"）
- 一句话：从零为 openGauss 构建了一套 **Iceberg 数据湖索引能力栈**
- 全栈交付：索引引擎（Rust）/ 桥接 / FDW（C++）/ Catalog / 内核 / 数据面（delta）
- 能力：向量索引（IVFPQ）+ 标量索引（BTree）+ 混合查询 + 索引管理
- 本轮形态：人主导 + AI（Claude Code）深度参与的全流程人机协作开发

## P2 背景与痛点（为什么做）
- 湖仓一体趋势：Iceberg 把数据放对象存储的 Parquet 文件里，但**湖上数据没有数据库式索引**
- openGauss 经 FDW 访问湖数据，只能**全表扫描**
- 痛点：百万~十亿级、128~960 维高维数据，全表扫描秒级延迟，无法支撑检索场景
- 一句话结论：必须在湖上数据上建立可查询的索引能力

## P3 交付范围（本次汇报的代码对象）
- 交付 = 全部产品代码仓（8 个）：引擎 / 桥接 / FDW / Catalog / **内核** / **delta 数据面** / **SDK 依赖**
- 表格：各仓职责一览（内核含 VectorSearch/TopK 集成，delta 含 planner hook）
- 强调：这是**完整索引栈**，不是单点功能；全项目 11 仓联编（8 产品 + 3 基建：data_infra / devtest / arrow-deps），C++/Rust/SQL 跨语言

## P4 技术挑战（难在哪）
- 架构从零搭建，ABI 严格对齐（跨语言、跨进程）
- 数据湖一致性：索引要与 Iceberg 生态（Puffin/metadata/manifest）一致
- 向量检索工程化：构建速度（GIST 曾 63min）、内存、召回率
- 与内核深度集成：规划器、异步任务、宏一致性（ENABLE_HTAP）
- 评测体系从零建：官方 GT 验证真实召回

## P5 目标（做到什么程度）
- 四大能力模块：
  1. 向量索引 IVFPQ（ANN 检索，官方 GT 验证召回）
  2. 标量索引 BTree（单列/多列复合）
  3. 混合查询（向量 + 标量，标量进检索内预过滤）
  4. 索引管理（create_index 异步、状态查询 API、查询闭环）

## P6 总体进展（一屏数字）
- 全栈产品代码全部落地合入主干（多 PR）
- 全量 devtest：67 通过 + 1 预期失败
- 召回率：SIFT 90.6%、GIST 78.4%（官方 GT @10）
- 延迟：秒级全表扫描 → 索引查询 0.2~1.5s（最高 11~13×）

## P7 关键里程碑（时间轴）
- 6 月底~7 月初：基建（联合编译 + 测试体系）
- 7 月：向量索引 IVFPQ + 官方 GT 召回 + vector→fixed 类型改造
- 7 月底~8 月初：性能调优（jemalloc / DOP / 并行回表 / 零拷贝）
- 8 月初~中：标量索引 BTree 多列复合 + 权限治理
- 8 月中~底：混合查询两阶段物化 + 异步建索引 + 索引查询 API
- 8 月底：devtest 全绿、问题闭环、环境清理

## P8 协作模式（AI 辅助开发流程）
- 一句话：人主导方向与决策，AI 承担执行层，全程对话迭代
- 全流程 8 环节：需求 → 调研 → 方案 → 评审 → 实现 → 测试 → 性能 → 检视合入
- 分工一句话：AI 读代码/写代码/跑测试/写文档；人定方向/定范围/验收/合入

## P9 AI 实际承担的工作（对照表）
- 表格：环节 ×（AI 承担） ×（人把关）
- 亮点：11 仓代码导航 / 系统性排障 / 方案快速试错 / 经验跨会话沉淀
- 3 个代表产出：调研报告、全栈代码实现、自动化测试工具链

## P10 案例：一次系统性排障（讲故事）
- 症状：全量测试 4 处失败，INSERT 崩溃、后续用例全连不上
- 排障路径：最小复现 → 排除 FDW/Catalog → 对比数据面/索引面 → 定位到 delta 构建参数与 gaussdb ABI 不匹配
- 结果：修复编译脚本，67 用例全绿

## P11 协作边界（如实记录）
- 反例 1：AI 早期结论「混合查询优化收益仅 ~1%」→ 实为 scalar_filter 发射位置 bug，优化从未生效；修复后 11~14×
- 反例 2：测试漏开向量参数把全表扫误判为索引问题；检视修复曾未提交进 PR
- 结论：人与 AI 都会误判，靠严格对照实验纠错；方向与质量责任在人

## P12 优势与能力（AI 带来什么）
- 效率：大代码库导航 / 方案快速试错 / 跨会话经验复用
- 质量门禁不因 AI 而降级：devtest + 官方召回率 + A/B 对照
- 关键结论需人复核后才纳入报告

## P13 工程实践（怎么做）
- 联合编译：build.sh 一键 11 仓，宏一致性（ENABLE_HTAP）纳入维护
- 测试体系：devtest 67 用例 + 快照锁定、A/B 双分支、官方 GT 真实召回
- 数据：SIFT/GIST/MoReVec/DEEP-1B/LAION2B 评测数据集导入与验证
- 性能方法：perf 火焰图、DOP 扫描、缺页/锁归因

## P14 性能优化成果（核心数字页）
| 优化项 | 手段 | 效果 |
|---|---|---|
| IVFPQ 向量索引 | ANN 替代全表扫描 | SIFT 11×、GIST 13× |
| 两阶段物化 | 先读标量列→过滤→只物化存活行 | 1% 档 K10 快 11.2×、K100 快 13.8× |
| vector→fixed | 消除嵌套开销 | 1.2~1.7× |
| jemalloc 调优 | retain:true | 延迟 -17~37%、缺页 -57~62% |
| 并行回表 | rayon 并行 | GIST 热查询 -79% |
| DOP 并行 | 分区并行 | IVF 1→8 加速 4.1~4.8× |

## P15 一处关键纠错（可信度加分）
- 初版 A/B 收益仅 ~1% → 逐层核实发现 scalar_filter 发射位置错误、引擎未收到
- 修复后：~1% → 11~14×
- 教训：优化前必须先验证信号确实送达

## P16 成果汇总
- 完整能力栈：全栈产品代码落地合入，建索引 → Puffin 存储 → 检索 → 数据库查询闭环
- 索引能力：IVFPQ（官方召回 SIFT 90.6% / GIST 78.4%）、BTree 多列、混合查询、异步管理 API
- 性能：秒级全扫 → 索引查询 0.2~1.5s；混合查询最高 11~14×
- 质量：67 通过 + 1 预期失败，问题全闭环
- 沉淀：10+ 篇文档、28 类踩坑经验、可复用工具链与编译环境

## P17 后续规划
- 短期：bridge 锁粒度优化（全局锁 → per-file）+ 多线程化，释放 DOP>8 扩展性
- 中期：索引级候选集预过滤（精确档覆盖更高选择率）
- 远期：规划器成本估算 / 融合索引

## P18 经验与总结（金句页）
- 优化在真实瓶颈处发力：用户态 10+ 优化点零收益，回表物化一改即 11~14×
- 从零搭跨语言跨仓索引栈：系统性方法（联编/快照测试/最小复现/官方 GT）是质量地基
- AI 提效显著、质量责任在人：反直觉结论先核实再采信
- 经验要可沉淀可复用：记忆 + 文档 + 脚本，让踩坑成为资产

## 结束页
- 谢谢
- 联系方式 / Q&A
