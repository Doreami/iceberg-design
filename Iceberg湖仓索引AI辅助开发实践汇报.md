# Iceberg 湖仓索引开发实践（AI 辅助开发项目）汇报

> 项目：openGauss 数据湖索引能力栈 —— 索引引擎（iceberg-index）+ Rust 桥接（iceberg-rust-bridge）+ FDW（iceberg_fdw）+ Catalog（openGauss-Catalog）
> 周期：2026-06 底 — 2026-08（约两个月）
> 形态：人主导 + AI（Claude Code）深度参与的全流程人机协作开发

---

## 一、背景与挑战

### 1.1 背景

湖仓一体趋势下，Iceberg 作为开放数据湖格式，将数据放在对象存储 / 文件系统的 Parquet 文件中，具备事务与 Schema 演化能力，但**湖上数据天然没有数据库式的索引结构**。

openGauss 通过 FDW 接入 Iceberg 数据湖，但查询只能走全表扫描（full scan）。面对百万乃至十亿级、128~960 维的高维数据，全表扫描级别的查询延迟（秒级）远不能满足检索场景需要。

项目使命：**为 openGauss 构建一套完整的 Iceberg 数据湖索引能力**——覆盖建索引、索引存储、索引检索、以及与数据库查询引擎的完整集成链路。

### 1.2 交付对象（本次汇报的代码范围）

本次交付覆盖**全部产品代码仓**（7 个交付单元 / 8 个仓库），含内核集成与数据面；不含编译/测试基建仓（data_infra、DataInfra-devtest、iceberg-arrow-deps）。

| 分类 | 仓库 | 语言 | 职责 |
|---|---|---|---|
| 索引引擎 | iceberg-index | Rust | IVFPQ 向量索引、BTree 标量索引、混合查询两阶段物化，Puffin 索引文件存储 |
| 桥接 | iceberg-rust-bridge | Rust / C ABI | 引擎与数据库进程桥接，线程 runtime 管理，scalar_filter 透传 |
| FDW | iceberg_fdw | C++ | 类型映射、扫描适配、索引下推、scalar_filter 序列化 |
| Catalog | openGauss-Catalog | C++ / SQL | 建表建索引、元数据台账、异步建索引、权限治理 |
| 内核 | openGauss-server-datainfra | C/C++ | 向量检索规划器与执行集成（VectorSearch / TopK） |
| 数据面 | iceberg_delta | C++ | 查询执行扩展（planner hook），数据面加速 |
| SDK 依赖 | iceberg-rust-datainfra / iceberg-rust-cache | Rust | Iceberg SDK overlay 与磁盘缓存适配（桥接的 path 依赖） |

> 注：全项目参与构建共 11 个仓库，除上表 8 个产品仓外，另有 3 个基建仓——`data_infra`（编译编排/快照锁定）、`DataInfra-devtest`（devtest 全量回归）、`iceberg-arrow-deps`（Arrow 三方依赖），不属于本次交付的代码对象。

### 1.3 技术挑战

| 挑战 | 具体表现 |
|---|---|
| 架构从零搭建 | 引擎↔桥接↔FDW↔内核全链路新架构（+ delta 数据面 / SDK 依赖），跨 11 个仓库、C++/Rust/SQL 多语言，ABI 必须严格对齐 |
| 数据湖一致性 | 索引必须与 Iceberg 生态一致（Puffin 索引文件、metadata、manifest），而非数据库本地结构 |
| 向量检索工程化 | IVFPQ 构建/检索/召回率全链路；GIST 构建曾需 63 分钟，构建速度与内存都要优化 |
| 与内核深度集成 | 规划器、查询执行、异步任务、二进制宏一致性（ENABLE_HTAP） |
| 评测体系从零建 | 需权威数据集（SIFT/GIST/DEEP/MoReVec/LAION2B）+ 官方 GT 验证真实召回率 |
| 隐蔽缺陷多 | 项目沉淀 28 类踩坑经验，多数为文档无法预见的深层问题 |

---

## 二、目标与进展

### 2.1 目标

四大能力模块，构成完整数据湖索引链路：

1. **向量索引（IVFPQ）**：ANN 近似检索，召回率用官方 GT 文件验证；
2. **标量索引（BTree）**：单列 / 多列复合，支撑等值、范围过滤；
3. **混合查询**：向量 + 标量联合检索，标量条件进入向量检索内预过滤；
4. **索引管理**：create_index（支持异步）、索引状态查询 API、与 FDW 查询路径闭环。

### 2.2 总体进展

- 全栈产品代码全部落地并合入主干（引擎 / 桥接 / FDW / Catalog / 内核 / 数据面 多条 PR）；
- 全量端到端测试 **67 用例通过 + 1 预期失败**，并修复 4 处因构建参数引起的系统性失败；
- 召回率达标：**SIFT 90.6%、GIST 78.4%**（官方 GT @10）；
- 查询延迟从秒级全表扫描降至索引查询（SIFT 0.2 秒 / GIST 1.5 秒，详见第五章）。

### 2.3 关键里程碑

| 阶段 | 时间 | 内容 |
|---|---|---|
| 基建 | 6 月底~7 月初 | 多仓联合编译环境、数据导入与召回率测试体系 |
| 向量索引 | 7 月 | IVFPQ 落地、官方 GT 召回评测、vector→fixed 类型改造 |
| 性能调优 | 7 月底~8 月初 | jemalloc、DOP 并行查询、并行回表、索引零拷贝优化 |
| 标量索引 | 8 月初~中 | BTree 多列复合索引、权限治理 |
| 混合查询 + 管理 | 8 月中~底 | 两阶段物化（scalar-first）、异步建索引、索引查询 API |
| 收尾 | 8 月底 | 全量 devtest 全绿、遗留问题闭环、环境清理 |

---

## 三、AI 辅助开发流程

### 3.1 协作模式

采用「**人主导 + AI 深度参与**」的人机协作模式。全流程 8 个环节均有 AI 参与：需求拆解 → 技术调研 → 方案设计与对比 → 评审 → 代码实现 → 编译部署与测试 → 性能分析 → 检视修复与合入。

AI 承担执行层（读代码、写代码、跑测试、写文档），人承担决策层（定方向、定范围、验收、合入），每步通过对话即时反馈迭代，形成紧密循环。

### 3.2 AI 实际承担的工作（如实记录）

| 环节 | AI 承担 | 人把关 |
|---|---|---|
| 技术调研 | 检索并阅读 11 个仓的代码，产出调研报告与方案对比 | 设定调研范围、确认方向 |
| 方案设计 | 生成索引级 / 物化级等多个候选方案并对比取舍 | 选定方案、约束非目标范围 |
| 代码实现 | 编写全栈产品代码（引擎 / 桥接 / FDW / Catalog / 内核 / 数据面） | 代码检视、指出问题 |
| 编译部署 | 维护多仓联合编译脚本，定位构建失败根因 | 提供环境上下文 |
| 测试 | 编写导入/基准/召回率脚本，分析 devtest 失败根因 | 验证结论、复核结果 |
| 性能分析 | perf 火焰图解读、A/B 数据对比与结论撰写 | 判断取舍、决定是否继续投入 |
| 文档 | 设计文档、评审意见回复、踩坑经验沉淀 | 审阅、决定合入 |
| 排障 | 从构建参数一路追到 ABI 不匹配等根因 | 确认复现与修复 |

### 3.3 代表性案例：一次系统性排障

全量测试出现 4 处失败，症状是「带字面量的 INSERT 报 OPT_PLANNER 崩溃、后续用例全连不上」。AI 按排除法逐层收敛：

1. 先用最小复现（仅建裸 FDW + 一条 INSERT）排除 FDW / Catalog 版本因素；
2. 对比数据面（全扫正常）与索引面（崩溃），结合 gaussdb 二进制符号比对，定位到 **delta 扩展的构建参数（ENABLE_HTAP）与 gaussdb 二进制 ABI 不匹配**——C++ 头文件结构布局不同导致扩展写坏查询树；
3. 修复编译脚本、重编 delta、全量重测 → 4 处失败全消，67 用例全绿。

这个案例体现了 AI 在多仓大代码库中「系统化假设验证 + 最小复现」的排障能力。

### 3.4 协作边界（如实记录）

- **AI 的结论会被数据推翻**。混合查询早期 A/B 曾得出「优化收益仅 ~1%」的结论，随后发现是 FDW 把 `scalar_filter` 发射到了配置对象内部、引擎从未收到，优化根本没生效；修复后同场景收益 11~14×。**人与 AI 都会误判，关键是靠严谨的对照实验纠错**。
- **人的把关不可替代**。期间出现过「测试漏开向量参数、把全表扫误判为索引问题」以及「检视修复只在本地工作树、未提交进 PR」两类人为疏漏，均由人在评审环节及时纠正。
- AI 的价值在于**提效与兜底**（快速实现、系统性排查、经验沉淀），方向与质量责任始终在人。

---

## 四、优势与能力

### 4.1 AI 辅助带来的能力提升

- **大代码库导航**：跨 11 个仓库定位函数、理解调用链，数秒完成人工需要数小时的事；
- **系统性排查**：面对跨层 bug 时逐层假设验证，产出可复现的最小用例；
- **方案快速试错**：同一问题可快速产出多版本实现做 A/B，收敛到最优解；
- **经验跨会话沉淀**：28 类踩坑经验以结构化记忆保存，任何一次新会话都能复用，不随会话结束丢失；
- **文档与代码一致性**：设计文档、测试报告、脚本说明自动维护，避免文档漂移。

### 4.2 边界与依赖

- 依赖人对**业务方向与范围**的判定——非目标（如多查询向量批量检索、融合索引）由人显式排除；
- 依赖严格的质量门禁——**全量 devtest + 官方召回率 + A/B 对照**作为合入前提，不因 AI 实现效率高而跳过验证；
- 关键结论需人在数据上复核——本项目中由 AI 提出的性能结论，均经人工复核后才纳入报告。

---

## 五、开发实践

### 5.1 工程实践

- **联合编译**：`build.sh` 一键编译 11 个仓库，支持增量/全量/单仓；构建参数与 gaussdb 二进制的宏一致性（ENABLE_HTAP）纳入维护范围；
- **测试体系**：devtest 全量用例（67 用例 + 快照锁定）；A/B 双分支对照；官方 GT 文件 + 官方 query 计算真实召回率；
- **数据工程**：SIFT/GIST（百万级 128/960 维）、MoReVec（真实过滤负载）、DEEP-1B、LAION2B 等评测数据集的导入与验证脚本；
- **性能方法论**：perf 火焰图（冷/热两种测法）、DOP 扩展性扫描、缺页/锁竞争归因。

### 5.2 性能优化实践（量化结果）

| 优化项 | 手段 | 实测效果 |
|---|---|---|
| **IVFPQ 向量索引** | ANN 检索替代全表扫描 | SIFT 全扫 2358ms → 索引 212ms（**11×**）；GIST 18883ms → 1467ms（**13×**） |
| **两阶段物化（scalar-first）** | 物化路径拆为「先读标量列→过滤→只物化存活行」 | 1% 过滤档：K10 快 **11.2×**、K100 快 **13.8×**；5% 档 7.2×；无过滤无回归 |
| **vector→fixed 类型改造** | 向量列改 Iceberg fixed 存储，消除嵌套开销 | SIFT 1.7×、GIST 1.2×、FullScan 1.4× |
| **jemalloc 内存调优** | `MALLOC_CONF=retain:true` 消除 TLB shootdown 与重复缺页 | 延迟 -17%~37%，缺页 -57%~62% |
| **并行回表** | 回表物化 rayon 并行化 | GIST 热查询 -79% |
| **DOP 并行查询** | 分区并行 + IVF 距离计算并行 | IVF DOP 1→8 加速 4.1×~4.8×；确定最优配置 16 分区 + DOP=8 |
| **索引零拷贝系列** | 构建/读取路径消除重复拷贝、原生 FileIO 区间读 | 索引构建与读取吞吐提升（多项 PR） |

### 5.3 一处关键纠错

混合查询物化路径初版 A/B 收益仅 ~1%，一度判断为「瓶颈不在物化」。后经代码逐层核实，发现是 **`scalar_filter` 被 FDW 发射到索引配置对象内部**、桥接从顶层解析而从未收到，优化逻辑实际未生效。修正发射位置后复测，收益从 ~1% 变为 11~14×。这一纠错印证了「优化前必须先验证信号确实送达」的测试原则，也展示了人机协作下对中间结论的反复核实。

---

## 六、成果与规划

### 6.1 成果

- **完整能力栈**：全栈产品代码（引擎 / 桥接 / FDW / Catalog / 内核 / 数据面）全部落地合入，形成「建索引 → 索引存储（Puffin）→ 索引检索 → 数据库查询闭环」的完整链路；
- **索引能力**：IVFPQ 向量索引（官方 GT 召回 SIFT 90.6% / GIST 78.4%）、BTree 单列/多列复合标量索引、向量+标量混合查询加速、异步建索引与索引状态查询 API；
- **性能**：向量查询从秒级全表扫描降至索引查询（SIFT 0.2 秒 / GIST 1.5 秒，最高 11~13×）；混合查询最高 11~14×；DOP 并行 4.1~4.8×；类型改造 1.2~1.7×；
- **质量**：全量 devtest **67 通过 + 1 预期失败**，遗留问题全部闭环；
- **沉淀**：10+ 篇需求/设计/测试/评审文档、28 类踩坑经验、可复用的数据集导入与性能测试工具链、多仓联合编译环境。

### 6.2 规划

| 优先级 | 方向 | 说明 |
|---|---|---|
| 短期 | bridge 锁粒度优化 | 现桥接为进程级全局锁，将降为 per-file 粒度并多线程化 runtime，释放 DOP>8 扩展性 |
| 中期 | 索引级候选集预过滤 | 精确档能力覆盖到更高选择率场景（候选集在检索层收窄） |
| 远期 | 规划器成本估算 / 融合索引 | 由成本模型自动选择混合路径；标量+向量融合索引 |

---

## 七、经验与总结

- **优化要在真实瓶颈处发力**。用户态 10+ 个优化点逐一实测全部零收益，而回表物化一改即得 11~14×——性能工作的前提是先用数据把瓶颈钉死，而不是凭直觉铺开。
- **A/B 对照必须严格同流程**。一次信号发射位置的 bug 曾让整个优化"假失效"，差点被当成负面结论丢弃；对照组、预热、GUC 开关等细节必须逐项一致。
- **从零搭一套跨语言、跨仓库的索引栈，系统性方法比单点聪明更重要**。联合编译、快照锁定测试、最小复现排障、官方 GT 评测，构成保证质量的地基。
- **AI 辅助显著提效，但方向与质量责任在人**。AI 负责快速实现、系统排障、经验沉淀，范围取舍、测试门禁、检视合入始终由人决策；AI 给出反直觉结论（如"优化无收益"）时，先核实信号链路而非直接采信。
- **经验要可沉淀、可复用**。结构化记忆、文档、自动化脚本三者结合，让两个月的踩坑成为后续迭代的资产，而非一次性教训。
